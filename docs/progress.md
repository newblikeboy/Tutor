# Progress

## 2026-09-26: deployment migration account resolution

The operator's deployment stopped before switching releases. Re-running the migration through systemd returned status 217 (user-credential setup), before the migration binary executed. The deployment script had hard-coded `User=tutor` and `Group=tutor` instead of inspecting the existing service. It now resolves the configured `tutor-api` user, group and supplementary groups before any build, validates the account/group, and uses those credentials for the transient migration. An omitted group uses that user's primary group; an omitted system-service user preserves systemd's existing root default. A missing named user never falls back to root. Dynamic users and failed lookups stop with a clear preflight error. No service accounts or private settings are edited.

Bash syntax and isolated account-resolution tests passed for named user/group, primary-group fallback, supplementary groups, the existing system default, missing user/group, DynamicUser and failed property lookup. These tests stub commands and do not modify services/accounts. Live Linux execution of the corrected deployment remains pending operator output. Reference: [systemd exit-status definitions](https://github.com/systemd/systemd/blob/main/man/systemd.exec.xml).

## 2026-09-26: simplify Updates and remove end-to-end encryption

The latest operator request supersedes the encrypted-inbox design below. Updates opens directly after account sign-in; admins can send to parent/tutor accounts before the recipient opens Updates. Approved tutors remain limited to parents in their current tuition assignments. Inbox activation, passphrases, device linking, public-key APIs, encryption code and encryption labels are removed. Recipients cannot reply and only the authenticated recipient can acknowledge a read; the first server timestamp remains immutable.

New subject/body records live in `inbox_messages`. The additive `007-simple-inbox` migration preserves all legacy encrypted collections without changing or pretending to decrypt them. Legacy ciphertext is not displayed in the ordinary inbox or counted as new unread messages. Production HTTPS, sessions/CSRF, rate limits, account/assignment guards, no-store headers and audit metadata remain. Sender retries retain the same draft, nonce and idempotency key. See [current design and migration](updates-inbox.md).

Completed checks: strict TypeScript, lint, three frontend unit/component tests, production build, full local-Mongo Go race suite and vet. Six Updates API scenarios cover immediate sending, legacy retention, removed endpoints, validation, cross-instance idempotency, recipient-only reads/CSRF, applicant reception, household isolation, tutor revocation/suspension and pagination. The focused follow-up also passed for guardian ownership changes in tutor lists. The actual inbox browser flow and existing billing/tuition-conversation/Activity regression passed (2/2). Nine screenshots were individually reviewed with axe, 360–1440px reflow and CSS 200% text checks. Browser tests cover send-before-first-visit, lost-response retries, failed detail fetches staying unread, reload and another authenticated browser opening the same message without setup. [Visual evidence](visual-qa/updates/README.md). Contract comparison changed only Inbox definitions/routes, and deployment Bash syntax passed. No production deployment or real messages were performed.


## 2026-09-26: one-way encrypted Updates inbox

The operator clarified that Updates must be one-way, show read receipts to senders, and use end-to-end encryption. Implemented administrator-to-parent/tutor sends and approved-tutor-to-current-parent sends, received/sent lists, encrypted subject previews, unread counts, compose/detail screens, recipient activation and signed read receipts. Parents have no compose/reply action. Applicants can receive administrative updates. Existing tuition chat and automated Activity alerts remain separate. Server checks retain role/assignment boundaries and revoke former-tutor access.

Web Crypto encrypts subjects and bodies for sender/recipient, signs envelopes and read receipts, and protects each immutable private-key vault with a separate client-only inbox passphrase. Unlocked keys/cleartext remain in component memory; crypto work avoids the global mutation cache. Setup explains that lost passphrases cannot be reset/recovered. The documented protocol has no forward secrecy, automatic key transparency or independent audit; see [encryption design and limitations](encrypted-inbox.md).

Verification completed on local test databases only: full `go test -race ./...` and `go vet ./...`; eight Vitest tests (five new crypto tests plus three existing component tests); strict TypeScript/lint and production build; real browser inbox + existing tuition/billing/conversation workflows (2/2); support/Activity regression; and reviewed workspace visual comparisons. The inbox browser test includes a response lost after server commit, successful retry without duplicates, wrong passphrase, encrypted payload inspection, reload recovery, no replies, tamper rejection and signed read-status persistence. Ten inbox captures were individually reviewed with axe, 360–1440px reflow, CSS 200% text reflow and keyboard lock checks. Private-cache inspection found no inbox plaintext/passphrases or globally cached crypto mutations. [Visual evidence](visual-qa/encrypted-inbox/README.md).

An initial crypto unit run hit its five-second timeout during multiple deliberate PBKDF2 operations while Go race compilation was active; the bounded test timeout was raised to 20 seconds, with the 600,000-iteration work factor unchanged, and all eight tests passed. The desktop workspace visual test detected only the intended 132-pixel Updates icon change; the candidate/diff were reviewed before replacing that one baseline. Mobile was unchanged. A local extra QA harness initially used Playwright's implicit browser-page context, which axe rejects; switching the harness to an explicit context allowed the checks to pass.

Added idempotent `--inbox-only` migration for two new collections, validators and indexes. The operator-run existing-droplet deployment script applies this narrow additive migration through systemd before switching releases, preserving private settings and old-release rollback. Bash syntax passed; actual Linux systemd deployment/rollback, real-account activation and independent crypto review remain unexecuted here. No production data, real messages or external delivery services were used.

## 2026-09-25: operator-requested GitHub release and deployment command

The operator requested pushing the completed brand/homepage/mobile-menu changes to GitHub and a single droplet deployment command. Added `scripts/deploy-existing.sh` for the existing symlink-based `/srv/tutor` deployment. It builds the selected commit with limited concurrency, keeps the running release during compilation, retains old assets for open browser tabs, switches releases atomically, and checks HTTPS readiness, GyanSetu production configuration and the exact served frontend. Activation/check failure attempts to restore the previous release. Existing production environment, Nginx/TLS, data and provider settings are preserved; no migrations or seed actions are run for this presentation release.

Bash syntax and diff checks passed; the private-value scan found zero matches in 270 text files against six private values. A local isolated rollback harness could not proceed because Windows denied creating its test symlink; no service or production path was touched by that attempt. Actual Linux deployment/rollback remain unexecuted here and require the operator's command output. Application build/lint, prior functional/visual checks and the mobile-menu browser check are recorded below. GitHub publication uses the existing `origin/main` without force-pushing.

## 2026-09-25: mobile public menu sign-in link

Fixed the public mobile drawer's unconditional workspace link: signed-out visitors now see Sign in linking directly to `/login`; authenticated visitors retain My workspace linking to `/workspace`, matching the desktop header. Hamburger placement and existing account design are unchanged. Build/strict TypeScript and lint passed. A real Chromium check against the isolated local Go/Mongo test environment verified both destinations, drawer closure on sign-in navigation, preserved desktop sign-in, no page errors and zero mobile-drawer axe violations. Signed-out/signed-in mobile and desktop screenshots were inspected in `visual-qa/gyansetu-home/landing-*sign*.png`. No production deployment or baseline replacement occurred.

## 2026-09-25: GyanSetu brand and homepage redesign

The user selected **GyanSetu** as the visible brand while retaining **thegyansetu.in** as the domain. Updated public/account/private wordmarks through the shared configuration, fallback copy, page metadata, favicon title, calendar producer, checkout label, Go default, API contract title and current setup instructions. The exact legacy APP_NAME value is normalized to the new brand in Go and the frontend, so an older environment/API cannot reintroduce the previous name. Domain names, package identifiers, account data and unrelated changes remain intact; private environment files were not edited.

The homepage now uses the exact requested headline, supporting text and primary/secondary button labels. A wider editorial text column, a simpler rounded image frame, Home/Online labels and a compact glass caption improve hierarchy; mobile actions stack at full width. Matching and application actions retain their private-route return destinations and account intent. Tutor browsing remains in the real API-backed discovery section with loading/error/retry and empty states. The AI provenance label, paper/ink/sage palette and existing login/signup layouts are preserved. No update-design skill or new raster asset was used.

Validation: final production build/strict TypeScript, lint, three component tests, Go configuration tests/vet, formatting and diff checks passed. Three homepage Chromium checks passed in 1.5m against the real Go API and isolated local MongoDB, covering 360/390/768/1024/1440 widths, axe, 200% reflow, keyboard/FAQ/menu, parent/tutor/profile entry points, failed-request recovery, reduced motion and forced colours. Thirteen public/account/workspace/application captures, full-page homepage overviews and state/navigation captures were visually inspected; ten changed Windows baselines were promoted after review, with the mobile workspace baseline unchanged. All eleven visual comparisons then passed in 1.8m. The final image sizing uses a sharper desktop source and the homepage captures were reinspected. A browser smoke check verified the application title, mobile drawer brand, title restoration and favicon. Private-value scan: 268 text files, six private values, zero findings.

Evidence: [visual review](visual-qa/gyansetu-home/README.md). Test database `tutor_e2e_1790358497436` is local and retained; no Atlas mutation, public deployment, live provider action or password/auth-policy change occurred. The local test preview uses port 5174. Deploy the rebuilt API/frontend to update the live site; this checkpoint does not claim that deployment, broader workflow regression or cross-browser review was performed.

## 2026-09-25: custom domain and HTTPS verified

The operator connected `thegyansetu.in` to DigitalOcean DNS and ran the supplied droplet configuration script. Public DNS resolves the root and `www` to `143.110.177.39`. The script backs up Nginx/application environment settings, obtains a webroot certificate for both names, sets the canonical HTTPS origin, redirects `www`, and restarts the existing API. Operator output reports success and backups at `/root/thegyansetu-domain-backup.G4rm0O`; Certbot reports scheduled renewal with an Nginx reload hook. The first local HTTPS probe briefly saw the old certificate before its retry succeeded.

Independent external verification passed: HTTP on both names and HTTPS on `www` reach `https://thegyansetu.in/`; the canonical HTTPS page serves TheGyanSetu HTML. TLS hostname validation succeeds for both names, with certificate expiry 2026-12-24. Health and readiness return HTTP 200 (`ok`/`ready`); public configuration reports `authEnabled: true`, `development: false`, Cloudinary uploads enabled and payments disabled. Actual account sign-in, browser workflows and a renewal dry run were not exercised in this checkpoint; the prior staff binary deployment concern is not resolved by a domain change. No application build, database mutation or real provider delivery was performed by these verification tools.

## 2026-09-25: owner-authorized live account and media reset

The operator explicitly requested deletion of all application user data, confirmed the live database as `tutor_dev`, and chose deletion of its Cloudinary uploads. A bounded, one-off Go tool used the project Argon2id implementation and a MongoDB transaction to remove 52 old documents (including 13 accounts) and provision exactly one non-sample administrator, parent and tutor with the privately supplied password. Existing collections, indexes, validators and migration marker were preserved. Three provisioning audit entries were created. No other database was modified. Personal account details and password were not written to source files.

Cloudinary confirmed deletion of the three inventoried application assets; invalidation was requested and all nine application-prefix/resource-type inventory checks returned zero assets. Initial verification found exactly three users/credentials and zero old application records. A subsequent check found one new draft application and active session after the reset; these new records were preserved. Learner and file collections remain empty. Live HTTPS parent/tutor login, persisted session and logout checks passed. The first verification harness omitted the required JSON content type for logout; corrected checks passed and its residual session was removed. Administrator login still returns `503 staff_auth_unavailable`: the running droplet API predates the already-pushed staff-login fix. Operator deployment/restart remains required; no server shell access is available here. Local ignored operator evidence contains counts only, with no plaintext password.

## 2026-09-25: eight-character password minimum

The user requested a minimum of eight characters everywhere. The shared Go password validator, signup and password-change forms, staff provisioning errors, API contract generators and current documentation now use 8-128 characters. Existing hashes and longer passwords remain valid; no reset, configuration change or database migration is required. Argon2id, the existing common-password checks, session revocation and staff role boundaries remain unchanged.

Validation: targeted Go race tests for password bounds, real-Mongo account security, production password accounts and staff provisioning passed, as did Go vet, the final production web build/strict TypeScript, lint and three component tests. Two browser regressions passed against local MongoDB, covering eight-character signup/change/login, seven-character rejection and existing account/session security. Production API tests exercise eight-character parent/tutor passwords and all four provisioned staff roles. Four desktop/mobile screenshots were individually inspected with axe/reflow checks; the two signup baselines were explicitly reviewed before replacement. All four reviewed login/signup visual comparisons passed (53.6s). Fixtures remain in local MongoDB, never Atlas. Deployment of the rebuilt API/frontend is required on the droplet.

## 2026-09-25: operator-selected password-only staff login

The user explicitly chose option 2, overriding the previous staff MFA requirement. Production password authentication now accepts non-sample provisioned admin, mentor, support and finance accounts, and the staff tab renders the existing email/password form. Unknown roles and sample identities remain rejected. Public signup still accepts only parent/tutor, and all role/ownership/assignment checks remain. The provisioning CLI no longer prints the obsolete MFA gate message; it still requires a hidden password or explicit secret-manager stdin, refuses existing accounts and records an audit event.

Validation: complete local-replica-set Go race suite and vet passed (app 48.531s); web build/strict TypeScript, lint and three component tests passed. Expanded production API tests provision all four staff roles, verify login/session/logout, and reject public staff signup and support/finance academic access. The first assertion incorrectly expected a support/finance academic dashboard; it was corrected to expect the existing 403. A local HTTPS browser run using the built production API/frontend and the actual staff CLI passed parent/tutor signup/login plus all four staff login/reload/role-boundary/logout journeys. All six account views passed axe/reflow with no browser page errors. Two new staff captures were individually inspected; four public-form captures exactly match the prior reviewed images. The local browser harness initially needed a string-replacement fix and a stable `/apply` expectation for unapproved tutors; the final complete run passed.

All fixtures are in retained local MongoDB databases, not Atlas. No live staff account, new Linux user or external delivery was created by these tools. Current configuration remains `APP_ENV=production`, `AUTH_PROVIDER=password`; no new setting or migration is required. Deploy the new binary/frontend, then provision the operator's real account on the droplet using its private environment and hidden password prompt. MFA is not implemented or required under this override; email recovery, guardian verification and live-payment boundaries are unchanged. See [production authentication](production-auth.md) and [staff provisioning](staff-provisioning.md).

## 2026-09-25: production parent/tutor email-password access

The operator requested working signup/login on the HTTPS droplet after the previous release's blanket production gate caused disabled forms and failed startup with `AUTH_PROVIDER=password`. Production now accepts an explicit password provider for non-sample parent/tutor accounts. Missing/disabled configuration remains closed. Exact HTTPS origin and loopback binding are validated; Nginx's overwritten single client IP is used for per-client limits. Production sessions use a distinct method, preventing replay of development/legacy sessions. Sample and staff accounts are rejected at login and on every session/private request. Staff UI explains its separate MFA restriction. Existing cookie, Origin/CSRF, password rotation, tutor approval, guardian and payment checks remain.

Validation: full real local-replica-set `go test -race -count=1 ./...` and `go vet ./...` passed (app 67.434s); focused production API/config tests passed (app 6.352s, config 2.587s). Production web build/strict TypeScript and lint passed. Local Chromium exercised the built frontend and production Go API over HTTPS: both parent/tutor signup, login, logout and reload persistence passed; actual Secure/HttpOnly/SameSite cookie attributes checked. All six login/signup/staff desktop/mobile captures passed axe and reflow checks, with no browser page errors, and were individually inspected. No existing visual baseline replaced. API tests cover cross-instance persistence, password rotation/revocation, forbidden Origin/CSRF, staff/sample denial, session replay/expiry and trusted-proxy limits.

Additional checks: all six existing sign-in browser regressions passed in 2.0m, including parent/tutor/staff development login from localhost redirected to 127.0.0.1, session reload, private-route return navigation and negative Origin/CSRF. The npm wrapper consumed the requested grep flags, so the complete six-case login file ran successfully. Three component tests passed. Final config tests passed after adding explicit production opt-in; the production account test was rerun after making its rate-limit clock independent of the calendar date. Generated legacy captures were preserved under `.local/production-auth-alias-captures` and existing tracked captures restored. Private-value scan found zero matches.

All new verification used retained local MongoDB test databases, never Atlas. No production account, provider action or additional deletion occurred. The old droplet binary cannot use this setting; deploy the new release before enabling password auth. Staff MFA, email ownership/recovery, minor guardian verification and live payments remain outstanding; see [production authentication](production-auth.md). This checkpoint does not claim full platform production readiness.

## 2026-09-25: direct Cloudinary uploads and viewing

User override replaces the Cloudinary quarantine/scanner flow: photos, PDFs and MP4s upload from the browser directly to authenticated Cloudinary assets. Go issues bounded, non-overwritable signed intents after ownership/draft/quota checks, then verifies the exact provider resource using backend credentials before marking it ready. MongoDB stores canonical URLs, immutable asset references and access metadata only. Idempotent retries preserve the reserved object; expired intents, forged completion fields, mismatched metadata and closed drafts are rejected. Application photo/document selections continue to save immediately.

Authorised image/PDF viewing, downloads and MP4 playback redirect to five-minute signed Cloudinary URLs. The browser receives file bytes directly from Cloudinary. List DTOs omit stored URLs and provider object identifiers. New links recheck assignment; a link already issued remains usable until its short expiry. Native video has a fresh-link reload action. No ClamAV dependency or malware-scan claim applies to Cloudinary. Legacy disk/S3 behavior and production auth/payment restrictions remain intact. Zoom is unchanged. Existing Cloudinary evidence is verified lazily on view; nothing is deleted or publicly published.

Validation: full local-replica-set Go race suite passed (app 46.805s), Go vet passed, production web build/strict TypeScript/lint and all 3 component tests passed. Focused API security/config tests passed after the final strict completion-body changes (app 7.886s). Browser direct-upload test passed 1/1 in 1.1m with a real Go API/MongoDB and an explicitly isolated Cloudinary HTTP fixture, covering direct multipart uploads, reload persistence, ready references, inline image, PDF bytes, actual browser-generated MP4 playback/range delivery, logout access denial, mobile/desktop axe and overflow. Earlier protocol-only run also passed. Legacy private-file browser regression passed 1/1 in 1.5m. Desktop/mobile screenshots were individually inspected in `visual-qa/cloudinary-direct`; no baseline replaced.

Live read-only provider check: existing private Cloudinary PDF metadata lookup returned 200 with asset ID/version/size; the generated inline delivery URL returned 200 application/pdf. No new files were uploaded to the real account, no Zoom meeting was created, and the droplet was not changed by these tools. Actual direct browser uploads/playback from the droplet's HTTPS origin still need operator verification. No new collection/schema migration or private environment changes. Test databases and artifacts retained. New Linux deployment instructions no longer require ClamAV for Cloudinary.

A restart of the existing local API was rejected by automatic approval review with "blocked by policy". The old API remains healthy on 8080; a new binary was built in `.local/cloudinary-direct-api.exe`. Source/build changes must be deployed/restarted to use the new flow. Other local services were not replaced.


## 2026-09-24: TheGyanSetu rebrand

Replaced the provisional Tutor Platform name with the user-selected **TheGyanSetu** in public/auth/private wordmarks, fallback copy, checkout branding, calendar producer metadata, HTML title/description/application name, API defaults, deployment examples and project/API documentation. Added a paper/ink/sage book favicon using the established icon direction. The application step title now includes the brand and restores the previous page title when leaving the form. Only APP_NAME was changed in the private local environment; the local API was rebuilt/restarted and its live config endpoint confirms TheGyanSetu. Existing technical identifiers and calendar event UIDs remain stable.

Validation: production build/strict TypeScript, lint, Go config tests and Go API compilation pass. All 14 captured desktop/mobile views were individually inspected; the 11 applicable Windows baselines were promoted only after review. Existing visual comparisons pass **11/11 in 2.3m** against an isolated real Go/local-Mongo test environment. Browser smoke also verifies branded titles, no old visible brand, horizontal reflow, the mobile drawer, application-title cleanup and favicon delivery. An initial capture exposed the application-specific title override; it was corrected and recaptured. Evidence: [rebrand review](visual-qa/rebrand/README.md), `.local/rebrand-results`, and `.local/rebrand-report`. No deployment, external delivery, payment attempt or database/file deletion. Wider transactional suites were not rerun for this branding change.

## 2026-09-24: direct sign-in for private routes

Removed the signed-out privacy invitation page from the shared private shell. Visitors to matching or another private route now go directly to sign-in using history replacement, with the requested path and query preserved for the existing login/signup return flow. Session loading and retryable errors remain visible; server authentication and role gates are unchanged. Removed the website-return navigation from the remaining loading/error shell.

Validation: production build/strict TypeScript and lint pass. Three focused real Go/local-Mongo browser cases pass across the initial run and corrected rerun: homepage entry, desktop/mobile matching redirect and return through account choices, and workspace loading/retry/session failure/sign-out/support access boundaries. The focused rerun passed 2/2 in 50.3s after correcting test expectations for the first matching step and the staff support heading. An initial local database startup timeout was resolved using a temporary launcher with a longer startup allowance; no database content was deleted. Desktop/mobile sign-in captures in `visual-qa/direct-sign-in/` were visually inspected. Wider suites and Go tests were not rerun for this navigation-only change. No deployment or private configuration change.

## 2026-09-24: English-only interface

Removed public, sign-in and workspace language switches, the account language selector, all Hindi UI resource objects, Hindi-only styles and the Devanagari font dependency. Startup always resolves English and clears the obsolete browser preference; the workspace no longer applies saved display preferences. Account saves retain the existing API shape with English. Existing authored text and Hindi teaching-language/subject choices remain intact. Tests now expect English UI, including legacy Hindi browser/account preferences; current captures use `visual-qa/english-only/` and old bilingual evidence remains historical.

Validation: production build/strict TypeScript, lint, formatting and 3 component tests pass. **30 selected Chromium cases pass across the main run and focused rerun** against isolated real Go/local-Mongo databases. The first run passed 18 functional cases (14.8m total); its remaining checks reported nine missing new English baselines, two obsolete workspace-card selectors and one initial signup timeout. All eleven new Windows baselines were individually inspected; the workspace check now waits for the visible first-use heading. The focused rerun passed **12/12 in 2.1m**, including the unchanged-timeout account security case. It proves legacy browser/account Hindi preferences render English, old browser preferences are cleared, account saves persist English, and password/session controls retain their behavior. Other coverage includes role sign-in through both local addresses, signup, five application cases, accessibility/reflow, homepage error states and visual comparisons.

Evidence: [English-only review](visual-qa/english-only/README.md), eleven new `tests/e2e/baselines/english-only-*.png` files and `.local/english-final`. Education desktop/mobile, Teaching approach desktop and account-settings desktop/mobile captures were also inspected. Old bilingual evidence was preserved; current test selectors and capture names use English. Broader payment, file, staff and Go suites were not rerun for this interface-only change. No API/schema/private configuration change, deployment, file/database deletion or unrelated source reset.

## 2026-09-24: remove demo choice and worksheet from Teaching approach

Removed the demo preference, its recorded-demo fields/upload, the upload-unavailable note and the optional sample-worksheet control from step 5. The editable final review also omits those fields. The three written teaching questions and preferred interview times remain. Saves use the existing live interview path so legacy incomplete recorded drafts cannot be blocked by controls that no longer exist. Go's existing normalization clears obsolete demo-selection metadata; previously uploaded private files remain in Documents and retain quarantine/access checks. Historical submitted summaries and the backend contract remain compatible.

Validation: production build/strict TypeScript and lint passed. Focused Chromium regression passed **1/1 in 52.6s** against an isolated real Go/local-Mongo environment, covering the removed controls, English desktop/Hindi mobile axe and screenshots, Continue/reload/submission of a legacy recorded draft and retention of its private file with download still locked. Earlier test assertions were corrected to distinguish the three textareas from time inputs and to check preserved file records instead of obsolete demo-selection metadata. Both `visual-qa/application-compact/approach-simplified-*.png` screenshots were inspected. Wider suites were not rerun. Main app remains on 5173/8080; no API/schema/private configuration change, deployment or file/database deletion.


## 2026-09-24: compact tutor application tabs and field review

Replaced the nested application sidebar with six horizontal tabs, a native step-position progress bar and one visible form section. The form now uses the available workspace width, with smaller gaps, grouped education fields, paired document controls, compact subject choices and actions that do not cover fields. Mobile keeps six numbered tabs with full accessible names. Arrow/Home/End navigation and Enter/Space activation are supported. Save draft keeps the scroll position; step changes focus the heading and respect reduced motion.

Reviewed every tutor-application field and its conditional branches. English/Hindi changes clarify optional education documents, qualification examples, employment restrictions, availability, interview/demo preferences and staff-confirmed travel fees. Unset positive-only numeric fields render blank instead of 0; valid zero experience is retained and drafts still serialize numeric values to Go. Longer answers show character limits, the account email is visibly read-only, duration errors appear with their group and consent references point to the displayed notice. Existing draft/version/conflict, private upload, submission and approval behavior remains server-backed. No API/schema/configuration changes belong to this checkpoint.

Verification: production build/strict TypeScript, lint, all 3 component tests and private-value scan passed. Initial application Chromium run passed **5/5 in 3.7m**. Final expanded run passed 6/7 in 5.4m; the only failure was an obsolete exact Hindi document label in the upload test. After updating that selector to the new optional label, its rerun passed 1/1 in 59.9s: **all seven selected cases pass across the final run and targeted rerun**. Coverage includes keyboard tabs, blank numeric draft saves, five-width bilingual reflow/axe, 200% CSS text reflow, complete submission/staff review, stale drafts, locality correction, recorded demo, all private application uploads and application-first route gates. Formatting and git diff checks passed.

Visual review: user-supplied desktop and prior Hindi mobile screenshots were inspected before editing; final desktop/mobile views are under `visual-qa/application-compact/`. Review prompted fixed mobile step circles, shorter textareas, aligned time-row actions and grouped subject options. The field-by-field audit and inspected captures are recorded in that folder's README. No pixel baseline was promoted. Tests use the real Go API and isolated local replica-set database `tutor_e2e_1790265635430`; test data/files are retained. Temporary test API/Vite/Zoom/Mongo services were stopped; main frontend/API remain on 5173/8080 and readiness returned 200. Private `.env`, main Atlas records, login/signup design and release gates are unchanged. No public deployment, provider delivery/charges or database deletion occurred. The prohibited update-design skill was not used. Wider unrelated suites, Go tests, cross-browser and native screen-reader checks were not rerun for this frontend change.


## 2026-09-24: staff-confirmed tutor fees

User override: tutors never enter preferred fees. Online uses hourly pricing; Home Tuition uses weekly and monthly plans. The application now has six steps, with Teaching approach leading directly to Check & submit, and Teaching hours no longer has an editable fee. Legacy applicant rates are cleared on save and legacy availability prices are ignored. Staff save per-tutor INR prices during/after the interview, including the class count and duration in each Home plan. Approval requires a saved fee for the approved mode. Existing unpriced tutors show Fee to be confirmed until staff explicitly set prices.

Fees persist on the application with staff actor/time and a monotonically increasing revision; every update keeps an audit snapshot. Only administrators or assigned conflict-cleared reviewers can change them. Parent cards, profiles and trial selection show fee plans matching approved scope. Online agreement totals use the staff hourly price and class duration, rounded to paise. Stale price revisions are rejected, and later updates preserve existing agreements. No new collection/migration or configuration change is needed. Seeded examples have explicit free test prices; runtime never invents a price for an existing tutor.

The existing activation service still supports Online Mathematics, classes 6–10. Home weekly/monthly fees are stored and reviewable; this change does not introduce Home approval, package booking or payment collection. Existing Cloudinary photo/resume/education upload work is preserved.

Verification: real local replica-set `go test -race ./...` passed (app package 46.897s), including new role/stage/fee validation, required fee before approval, cross-instance persistence, public scope filtering, rejection of tutor fee writes, stale price quotes, 30/90-minute calculations and unchanged old agreements. Go vet, generated contract, strict TypeScript, lint, production build and all 3 component tests passed. The first browser run exposed obsolete seventh-step selectors; those selectors are corrected.

Selected Chromium verification passed **10/10 in 9.3m**: application submission/stale drafts/locality correction/recorded demo, private uploads, staff interview/fees/approval/lifecycle and stale decisions, disabled payment/conversation flow, recurring tuition/learning plan/handover and empty/error states. New staff fees persist across reload, all three values match the API, parents see the approved hourly price, and sampled axe/overflow checks pass in English/Hindi. Eight final screenshots were individually inspected under `docs/visual-qa/staff-fees/`; unrelated generated images are restored and no pixel baselines promoted.

The added changed-price browser regression then passed **1/1 in 1.9m** against a fresh isolated database. A staff fee revision while the family has a quote open returns 409, refreshes the current offer and clears the family's confirmation; confirming again successfully continues through recurring tuition, academic review and consented handover. Production build/TypeScript, lint, 3 component tests and the private-value scan also passed. The complete wider Chromium/pixel suite was not rerun; no cross-browser or native screen-reader verification is claimed.

Main Go API rebuilt to `.local/fees-api.exe`, restarted, and readiness through port 5173 returned 200. Main frontend/API remain 5173/8080. Private `.env` is unchanged; the private-value scan found no matches. No live provider action, deployment, or retained database deletion occurred.

## 2026-09-24: passport-size photo and application upload provisions

About you now includes an optional passport-size JPG/PNG photo, limited to 3 MiB, using the existing private Cloudinary document adapter. The Education step retains its resume and up to six educational documents, with simpler English labels. Upload success immediately saves the selected file ID in MongoDB; refresh, final application review and the staff Documents tab retain the attachments. English/Hindi labels and concise format limits use the existing form layout. Older applications without a photo still load, and photos never become public tutor-profile images automatically.

Go now persists `about.photoFileId`, bounds it and checks ownership, application target, scan state and image type on draft saves and submission. PDF/video/missing links are rejected. The generated contract/defaults and file-error translations are updated. Existing approvals, private storage, session/CSRF checks and actual scan gates are preserved; no new collection or migration is needed.

Validation: web production build/strict TypeScript, lint and all 3 component tests passed. Focused application/Cloudinary-document MongoDB tests and media/provider tests passed under the race detector; `go vet ./...` passed. Coverage includes JPEG/PNG photo persistence, PDF/video/missing-file rejection, wrong uploader/target, rejected files, final submission, idempotent uploads and quarantine. The first expanded fixture hit the existing upload rate limit; retry checks now exercise each storage route once without changing runtime limits. The expanded upload browser case passed its initial run (1/1, 54.8s). Final selected browser results and visual evidence are recorded in the acceptance matrix and visual review index.

Final selected Chromium run passed **3/3 in 2.8m**: `npx playwright test --config .local/recruitment.config.ts tests/e2e/files.spec.ts tests/e2e/application.spec.ts --grep 'private application evidence|seven steps persist|recorded demo' --output .local/photo-final-browser-results`. This covers the complete seven-step application with optional files omitted, recorded-demo persistence, photo/resume/education uploads, refresh and submission, invalid-photo rejection before uploading, locked downloads, staff summary/Documents, Hindi axe and five-width photo reflow. New desktop/mobile captures were inspected under `docs/visual-qa/application-uploads/`; photo captures were retaken at the bottom of the form so the existing sticky actions do not obscure evidence in a full-page screenshot. Earlier unrelated application captures were restored from pre-run copies; no pixel baseline was promoted.

The main Go API was rebuilt/restarted and readiness returned 200; frontend/API remain on 5173/8080 with Cloudinary uploads enabled. Private `.env` was not edited. `CLAMAV_ADDRESS` remains unconfigured, so uploaded files stay quarantined and opening/downloading remains locked. Browser checks use real Go and isolated local MongoDB/disk files, while Cloudinary wire tests exercise the existing signed private provider protocol; no new live provider upload, meeting, delivery, deployment or retained-database deletion occurred. The private-value scan found no matches. The update-design skill was not used. The wider regression suite, other browsers and native screen readers were not rerun.

## 2026-09-24: distinctive landing-page design

Reworked the public homepage directly, keeping the approved paper/ink/sage palette. The hero now pairs larger Manrope/Georgia display type with an arched tutoring image, fine orbit lines, a translucent caption and original CSS crystal shapes. Hindi keeps the existing Devanagari typography. Three illustrated steps, an editorial tutor section, a deep green continuity panel with a layered illustrative record, a quieter FAQ and crystal-accent closing panel replace the uniform card treatment. Copy is shorter and simpler in both languages. The existing AI-generated image is reused and labelled; responsive image hints account for its taller crop so desktop loads a sharp original variant. Decorations use code and the existing icon set, with no new raster asset or external font request.

The homepage retains real Go-backed tutor discovery, loading/error/retry and empty states, profile links, matching/application entry points, FAQ keyboard behavior, mobile navigation and the skip link. Reduced motion disables entrance/hover animation; high contrast preserves content and action boundaries. The new section link works with keyboard navigation. No preview banner was added. Sample labels, genuine approval rules and release gates remain; account/private styles, Go code, database schemas and private configuration were not changed. The prohibited update-design skill was not used.

Validation: frontend production build/strict TypeScript, lint, all 3 component tests, formatting and private-value scan passed. **13 selected Chromium checks passed**: 3 homepage flows, 3 individually reviewed updated homepage pixel baselines, 3 unchanged public profile/empty baselines, and 4 unchanged login/signup baselines. Homepage coverage includes English/Hindi at 360/390/768/1024/1440, axe WCAG checks, keyboard skip/menu/FAQ, 200% reflow, real discovery/profile/matching navigation, failed-request recovery, reduced motion and forced colours. The final public run passed 9/9 in 1.8m; account baselines passed 4/4. Only the three homepage baseline images were manually promoted after individual review.

Before screenshots are retained under `.local/landing-design-before/`. Final bilingual desktop/mobile views, full-page captures, five section details and a high-contrast capture are in `docs/visual-qa/landing-editorial/`; the inspected views are recorded in its README entry. Read-only browser checks used the running main services and did not create application/user records. Frontend/API remain running at 5173/8080, with readiness returning 200. No public deployment, provider delivery, payment or database cleanup occurred. Cross-browser and native screen-reader checks were not run.

## 2026-09-24: application documents stored privately in Cloudinary

The résumé control existed but was hidden while document storage was disabled; education-document uploads were missing. Education and experience now shows both controls, with up to six education PDFs/JPGs/PNGs (3 MiB each). Resume, education and worksheet uploads use authenticated Cloudinary raw storage; introduction videos keep authenticated video storage. Original bytes, validated filename/type, provider and digest are retained. No provider secrets or signed URLs reach the browser. Each successful upload immediately saves its application link; batch retries keep completed links and use stable per-file idempotency keys. Disabled storage shows a concise unavailable state instead of silently removing the controls.

Go validates linked document ownership, application target, permitted status and document/video type on draft saves and final submission. Education IDs are bounded and unique; existing applications without them still load. Staff can see the uploads in the existing Documents tab. Academic approval and actual scan gates are unchanged. The generated contract includes the new field and now consistently reapplies the existing recruitment additions when regenerated.

Validation: full Go race suite against an explicit local MongoDB replica set and `go vet ./...` passed. Tests cover authenticated raw PDF/JPG/PNG signatures, exact bytes, rejected public assets/unsafe IDs, document-only provider credentials, storage routing, ownership/type/rejected-file checks, MongoDB persistence, idempotency, quarantine and staff retrieval after a test-only successful scan. Web strict typecheck, lint, build and 3 component tests passed. Six distinct selected Chromium flows passed across the initial run (5/6) and corrected file-flow rerun (1/1). The initial file failure was an invalid embedded PNG fixture correctly rejected by the server; the fixture now uses a valid browser-generated PNG. The file flow verifies resume plus two education documents, refresh persistence, invalid-file rejection, locked downloads, submission and staff visibility. Desktop/mobile English/Hindi captures and axe/reflow checks are recorded under `docs/visual-qa/application-documents/`.

Live provider verification: synthetic PDF, JPG and PNG uploads succeeded as authenticated raw assets, returned byte-for-byte identical originals and each denied unsigned access with HTTP 401. Three synthetic private assets are retained; no personal files were used. A read-only check found no existing main-database uploads requiring migration. The private `.env` now has `MEDIA_PROVIDER=cloudinary` and `VIDEO_PROVIDER=cloudinary`; the Go API was rebuilt/restarted and the main frontend/API remain on 5173/8080. A secret scan found no exposed private values. The final file-flow rerun passed (1/1, 38.9s) after shortening repetitive saved-file copy; all three final captures were individually inspected. Isolated test services were stopped with their databases/files retained. `CLAMAV_ADDRESS` is absent: uploads work, while downloads/playback remain quarantined pending a real scanner. No deployment, real meeting, invitation, retained-database deletion or scan bypass occurred.

## 2026-09-24: simpler parent and tutor tasks

Parent and tutor navigation now share Home, Trial lessons, Regular classes, Help and Account. Shorter English/Hindi labels replace session/scope/continuity terminology where families or tutors need to act. The existing sidebar, paper/ink/sage colours and reflective surfaces are retained. Repeated introductions, an extra tutor tools card and empty request panels are removed.

Account settings use Profile, Password and Sign-ins tabs with URL state, keyboard navigation, retained unfinished fields and deferred sign-in loading. Regular classes use the shared accessible tabs for classes, plans, messages, files, schedules/fees and tutor changes; visited forms retain unfinished work. Hidden message/file/handover panels do not refetch on invalidation. Tutor acceptance follows the schedule/fee review, and pause/end controls sit with that agreement instead of below every section.

Draft applications show their seven sections without a second four-stage tracker; mobile uses one section selector and reachable save/continue controls. Submitted applications separate status, the saved application and documents. Teaching availability uses a native date picker and removable dates off, prevents duplicate dates, and includes a selected date when Save is pressed. The existing Go availability endpoint persists these changes. Account form widths, mobile headings, card padding and tab spacing have been tightened. Approval, guardian consent, payment confirmation and file scan rules are unchanged; action-specific test/scanner disclosures remain.

Validation: production build (including strict TypeScript), lint and all 3 component tests pass. **17 distinct Chromium cases passed across focused runs and targeted reruns**: account security (1), seven-step application/draft conflict/422 recovery/private demo (4), application-first route guards (1), new account tab/date-picker regressions (2), billing/conversation draft retention (1), private documents (1), recurring classes/consented handover and empty/error states (2), parent setup/learner priority/reviewed feedback (3), and tutor navigation/family learner selection (2). These use the real Go API and isolated local MongoDB, with test-only payment/video/meeting configuration. Checks include English/Hindi axe, keyboard tabs and drawers, five viewport widths, and 200% reflow. Desktop/mobile captures were individually inspected; evidence is in `docs/visual-qa/product-ux/` and the parent regression folder.

Initial attempts exposed a transient request timeout, a draft interrupted by a development hot reload during edits, stale test selectors after navigation labels changed, and scheduling fixtures left over from the class workflow. Final application runs were made with source edits paused; parent trial tests now explicitly prepare their fixture's teaching hours through Go. All affected cases passed reruns. No baseline images were automatically replaced.

The local test database is `tutor_e2e_1790193400377`; its files are retained. Temporary test services were stopped after verification. Main frontend/API remain on 5173/8080 and both return HTTP 200. Five final home/message/account captures passed axe and reflow after the last spacing adjustments. No API/schema changes, public deployment or live Cloudinary/Zoom/payment calls belong to this checkpoint. Existing Atlas capacity and unconfigured scanning limitations are unchanged; frontend checks do not claim production release readiness.

## 2026-09-24: simpler parent workspace and matching flow

Parent navigation now uses Home, Learners, Trial lessons, Regular classes, Payments, Updates, Help and Account. Trial lessons groups Upcoming, Feedback and Cancelled records in accessible URL-backed tabs; the previous progress URL still opens Feedback. Home shows the selected learner and one next action or nearest trial. Repeated request panels, onboarding paragraphs and privacy/promotional cards were removed. The learner selector appears only for families with multiple learners. The paper/ink/sage palette retains bright highlights; login/signup styling is unchanged.

Matching exposes its four actual stages, removes the repeated help sidebar, and uses short English/Hindi labels and field errors. Add learner explicitly starts a new profile; Add learning needs carries the selected learner instead of reopening another learner's draft. Learner creation persists before review, and the submitted request ID replaces the form URL. Reload/reopening shows the saved request and active trial instead of another booking form. Cancelled/declined records leave Upcoming; completed lessons stay under Feedback and server-reviewed evidence remains required before parent visibility. Shared trial cards preserve tutor/mentor actions and let parents continue to Regular classes. Mandatory consent and booking terms remain.

Frontend typecheck, production build, lint and the 3 existing component tests passed; the private configuration scan found no exposed values. **Seven distinct selected Chromium cases passed** across the focused run and targeted reruns: parent setup/request/trial/cancel/reload, selected learner versus another draft/new learner/Hindi adult, tutor completion → mentor review → parent feedback, parent bilingual responsive navigation, loading/retry/session/sign-out, multi-learner ownership/selection, and the earlier Hindi adult journey. The six-case run passed 5/6; its retained fixture-name collision was corrected with a unique fictional learner and the feedback rerun passed 1/1 (50.4s). The separate family-selection regression passed 1/1. Earlier test issues were capture-before-content readiness and navigation before a mutation response; the tests now wait for visible content and committed status. The final screenshot-polish setup/cancellation rerun passed 1/1 (1.6m); build/lint passed again after that polish. Final cancelled-trial, mobile review, published-feedback and multiple-learner screenshots were individually reopened. Temporary test services were stopped; their local database files were retained.

All actions use the existing Go/Mongo endpoints on isolated local replica-set database `tutor_e2e_1790191654452`. Backend code, contracts, private credentials and database schemas are unchanged. No update-design skill, real provider calls, deployment or database deletion. Main :5173/:8080 returned HTTP 200 and private `.env` retains the operator's 00:18:24 timestamp. Captures are under `docs/visual-qa/parent-ux/`; no prior pixel baselines were promoted. Full unrelated browser/Go suites, cross-browser and native screen-reader checks were not rerun.

## 2026-09-24: staff navigation and tabs

Reviewed the staff workspace for redundant presentation. Applications now contains Applications/Interviews tabs; Tutor network contains Tutors/Follow-ups. This removes two top-level admin entries while retaining their workflows and existing URLs. Mentors access interviews within Assessments. Overview shows one queue at a time. Applicant detail separates Application, Review & decision, Documents and History; staff billing separates payment records and payment follow-ups. Academic decisions, tutor lifecycle, payment recovery, audit, support and account security remain available under their existing role checks.

Inactive read panels unmount, defer their requests and use existing query caching/cancellation. Review forms mount on first visit and retain unfinished input when hidden, with staff-member queries disabled until their panel is active. Selection is in the URL; reload/Back work and queue-specific status filters clear when changing queue type. Manual keyboard tabs support arrows, Home/End and Enter/Space. Panels have names and logical headings, including empty follow-up queues. Employment-review editing is collapsed when no additional review is pending; family-specific billing introductory copy is omitted for staff.

Frontend typecheck, production build, lint and all 3 existing unit tests passed. The private configuration scan found no exposed values and `git diff --check` passed. Initial new request-count assertions included StrictMode's cancelled development probes; traces showed one aborted and one successful read. The tests now count completed reads. Subsequent checks found and fixed an empty-queue heading-order issue and corrected a test's mentor navigation label. Backend code, provider settings, database schemas and private `.env` are unchanged in this UI checkpoint. No update-design skill, real meeting, delivery, deletion or deployment was used.

Final browser run passed **6/6 in 3.9m**: `npx playwright test --config .local/recruitment.config.ts tests/e2e/staff-tabs.spec.ts tests/e2e/staff.spec.ts --output .local/staff-tabs-final-results`. This covers deferred overview/network/file/history/payment requests, keyboard selection, URL filters/Back/reload, retained unfinished notes, finance boundaries, mentor navigation, actual Go/Mongo interview/approval/suspension/termination and stale-write recovery. English/Hindi captures, sampled axe and reflow checks passed; final interview, Hindi assessment and approved-state captures were reopened after the last UI adjustment. Main frontend/API returned 200 on :5173/:8080. Temporary test services were stopped and local database files retained. Full unrelated browser and Go suites were not rerun for this frontend-only change; no measured latency/throughput improvement is claimed.

## 2026-09-24: Zoom permission and host verified

After the operator added `meeting:read:list_meetings:admin`, a fresh Zoom OAuth token returned all five required meeting permissions. Authentication and a read-only meeting-list request for the configured `ZOOM_HOST_USER_ID` both returned HTTP 200, resolving the earlier 4711 error. Cloudinary read-only authentication also returned 200. The backend was restarted to clear cached OAuth tokens and load the private configuration. No credentials or returned meeting data were printed, `.env` was not edited, and no meeting was created. Live meeting creation remains unexercised; the scanner is still unconfigured, so video playback remains quarantined.

## 2026-09-24: application-first tutors, private Cloudinary video and Zoom interviews

New tutors now see the application journey and essential account/support/updates navigation. Teaching routes and Go APIs require an approved, unexpired scope. Four visible stages track completion, staff review, Zoom interview and approval; the seven saved form sections and Home/Online fields remain. Application polling refreshes decisions without replacing editable drafts. Suspension, termination and expiry close teaching access again.

Implemented authenticated Cloudinary MP4 uploads with backend signatures, persisted receipts, opaque object IDs and protected range playback. Actual configured scanning is mandatory before viewing. Application metadata is bounded to 20 embedded attachments in the existing application document, retaining legacy file reads. This avoids requiring another Atlas collection. An attempted separate media collection migration encountered Atlas's existing 500-collection limit; the final implementation instead applies the additive `-application-only` migration successfully to the existing 15-collection app. No retained database was deleted.

Staff date/duration selection queues Zoom S2S OAuth creation; confirmed links appear in both accounts. Rescheduling updates the same meeting; cancellation queues deletion. Durable attempt markers and provider lookup prevent blind duplicate creation after a lost response. Pending/failed operations, bounded worker retries and assigned-staff retry controls are visible. Applicant/reviewer/shared-host reservations are transactional. Assessment and scoped approval remain human decisions. The initial staff browser run exposed scorecard inputs resetting when background confirmation changed the application revision; preserving the form mount fixed it.

Verification: the full real local MongoDB `go test -race -count=1 ./...` passed (API 38.588s), and `go vet ./...` passed. Provider wire tests and six recruitment database subcases cover signatures, OAuth CRUD/recovery, scan/ownership/range access, teaching gates, concurrency, retries and approval. Final selected Chromium run passed **3/3 in 2.6m**: recorded video persistence, application-first access and complete staff Zoom/approval/suspension/termination. Build/typecheck/lint and all 3 web unit tests passed; a private-configuration scan found no leaked values. The final contract includes MP4/range responses and bilingual provider-history labels replace internal action IDs. Visual evidence is under `docs/visual-qa/recruitment`; review details and limitations are in its parent README. The full unrelated browser suite was not rerun.

The operator added provider credentials to private `.env`; the agent did not edit it. Cloudinary read-only authentication returned 200. Zoom OAuth returned 200; create/read/update/delete scopes are granted, but listing returned code 4711 because `meeting:read:list_meetings:admin` is missing. That permission is needed for uncertain-create recovery. No live meeting or applicant upload was performed. The main environment has no scanner, so uploads stay quarantined. See [setup and provider limits](recruitment-integrations.md). No public deployment, external invitation or paid provisioning occurred; the prohibited update-design skill was not used.

Final main-app checks: Go readiness returns 200; config enables Cloudinary videos and Zoom and accurately reports no scanner. Chromium opens the Tutor sign-in screen from both localhost and 127.0.0.1, canonicalizing to 127.0.0.1. The updated API is running on :8080 and existing Vite on :5173. English/Hindi Zoom-history labels were verified in the browser and their cropped captures inspected. Temporary E2E/provider/Mongo services were stopped with database files retained. Final secret scan found no private values in inspected source files; `git diff --check` passed.

## 2026-09-23: tutor application final-submit 422 fix

Traced the reported final-submit rejection to a blank trailing entry in Home Tuition service localities. A read-only check of the saved draft identified only `availability.home.localities`; no applicant values were logged and no saved application was changed or submitted. Go now trims each locality and removes blank lines before draft/submission validation and persistence. Required locality, count (12), and length (2–100 Unicode characters) checks remain enforced. The form shows an accessible English/Hindi error beside this textarea and checks it before continuing from Availability.

Verification: nine normalization/validation cases plus existing application validation and the real local Mongo application/eligibility integration passed with `-race` (8.714s); `go vet ./internal/app`, frontend production build/typecheck and lint passed. The focused Chromium regression passed (1/1, 38.1s), covering final-submit 422 navigation and field error, whitespace-only rejection, corrected blank-line draft persistence/reload and successful final submission with Home/Online modes and fees preserved. Desktop/mobile captures `docs/visual-qa/application/422-localities-en-*.png` were individually inspected; sampled axe and overflow assertions passed. The corrected validator accepted the existing saved draft in a read-only diagnostic; the temporary diagnostic test was removed. Updated main API runs on :8080 and Vite on :5173; readiness/config and both localhost/127.0.0.1 entrypoints return 200. No schema migration, main application write, private environment change or database deletion was needed.

## 2026-09-23: seven-step tutor application — implementation checkpoint

Replaced the single-panel tutor application with the supplied seven-section flow in the existing sage/paper private shell. Signup still uses email/password and opens `/apply`. Saved drafts retain structured contact, education, requested subject/class/board/language/mode areas, IST availability, Home Tuition travel/service localities, Online readiness, teaching responses, demonstration preference, fee expectations and separate declarations. Home/Online/both are actual per-area Mongo fields and are available to staff filters/detail; registration and requested areas never grant approved scope. Legacy approved records remain readable, and old draft education/experience/approach are carried forward.

Added tutor-owned GET/PUT endpoints, bounded draft/full submission validation, a dedicated optimistic form revision, and a server-timestamped notice receipt. Uploaded documents/video do not invalidate the form revision. Staff see the complete private application and can record employment eligibility with reason, reviewer and time; employed teachers/uncertain restrictions cannot be approved until cleared. Staff approval is restricted to the first requested assessment area and the existing supported Online Mathematics 6–10 activation scope. Home/other subject requests remain recorded for review. No OTP, public staff role, automatic approval, public fee publication or third-party meeting creation was introduced.

Application-only MP4 uploads (25 MiB) extend the configured private document pipeline; ordinary documents remain 3 MiB. Container checks never stand in for scanning. Resume/worksheet/demo IDs are checked for ownership/type/status at submission. The main app still has uploads disabled; applicants can choose a live demonstration. Draft conduct/data notices state that retention terms await release review. No production release, real delivery, paid service or database cleanup was performed. No update-design skill was used.

Verification: full real local MongoDB `go test -race -count=1 ./...` passed (API 32.761s), `go vet ./...` passed, frontend lint/build and all 3 unit tests passed. The seven-step browser flow, save/reload, conditional Home/Online sections, stale-tab protection, Hindi/mobile accessibility and recorded-demo quarantine/revision tests passed. Final aggregate browser results are recorded below. An initial application browser attempt was interrupted by formatter-triggered Vite reload and passed when rerun without source edits. The existing file test correctly rejected invalid content but needed its Hindi message expectation updated for the new application-specific limits.

The additive `-application-only` migration has been applied to the configured Atlas app: collection count remains 15, application schema and requested-mode index verified. Updated local frontend/API are running at 127.0.0.1:5173 and :8080; config returns 200 and own-application access correctly returns 401 without a session. Both localhost and 127.0.0.1 frontend entrypoints return 200. Remaining extension collections/scanner/provider configuration retain their prior release limits. See [application-form.md](application-form.md).

Final browser regression evidence: the selected 20-case run completed 19 passing checks in 10.7m, including the full application/approval/trial journey, all three new application cases, staff lifecycle/stale decisions, account signup/recovery/reflow and localhost/CSRF tests. The sole failure was the older file test's literal Hindi file-format wording; its corrected rerun passed (1/1, 29.9s), preserving the actual invalid-file/quarantine/access assertions. Requested teaching areas and server-approved teaching scope have separate labels in the final read views; account email is included in review, and reviewer details are retained. Final build/lint and isolated browser read checks cover that presentation refinement.

Final evidence capture refreshed all seven desktop steps, selected mobile steps, Hindi Availability/Review and the staff approved/requested-scope view from real isolated fixture drafts; browser runtime errors and horizontal overflows were zero. Desktop and mobile screenshots were inspected. The 105 pre-task root PNG artifacts were restored from their task-start backup, retaining new evidence under `docs/visual-qa/application`. Temporary test services were stopped with local database files retained; the updated main frontend/API remain running. Private `.env` was not edited.

## 2026-09-23: working staff recruitment and tutor administration

Implemented the requested staff workflow directly, without the prohibited update-design skill. The existing paper/ink/sage sidebar now provides Applications, Interviews, Tutor network, Follow-ups and Decision history. The overview shows actual authorized Mongo aggregates and upcoming appointments. Queues have server search/status/reviewer filters, URL state and 25-record cursor pages. Application detail includes the applicant's saved qualifications/approach, reviewer, interview, scores, feedback, authorized documents and chronological internal history. English/Hindi and responsive layouts are retained.

Provisioned admins or assigned mentors can claim applications with a no-conflict declaration, schedule/reschedule/cancel interviews, record nonattendance after the start, score six assessment dimensions, request changes, decline and approve a bounded six-month teaching scope. Admin assessors must choose an ongoing academic mentor. Admins can assign/change reviewers, suspend, reinstate an unexpired approval, terminate or reopen eligible records for fresh assessment. Internal notes and decision/evidence snapshots remain staff-only. Expected application versions prevent one operator overwriting another; reviewer/applicant interview guards contend across API instances.

Staff paste an existing HTTPS Zoom invitation link. The applicant sees the saved appointment and can download an ICS calendar event. This does not create a Zoom meeting or send invitations. Suspension/termination revokes sessions and denies teaching routes after login, while preserving own-account/application/support access. Existing trials, tuition, learning and financial records remain. One durable follow-up opens/reopens with actual affected-record counts; recording its outcome does not simulate transfers, refunds or cancellations. Termination is final. No public signup role, approval bypass, fake provider success or production release was added.

Backend evidence: `go test -race -count=1 ./...` and `go vet ./...` passed against the explicitly selected real local MongoDB replica set; API package completed in 80.325s. Seven new staff subtests cover negative roles, required/stale versions, reassignment/conflict checks, interview validation, simultaneous overlap from two servers, rollback/release, administrator approval with mentor assignment, private/audited evidence, session revocation, retained bookings, final termination, follow-up deduplication and pagination/search/aggregates. A browser-discovered percent-encoded follow-up identifier issue was fixed, added to the database regression, and the focused `TestMongoStaffOperations` race rerun plus vet passed afterward (9.794s). Typecheck, lint, 3 component tests, production build, contract generation, changed-code Prettier and `git diff --check` passed. The limited private-config scan checked 198 text files against three private values with zero findings.

Chromium evidence: selected `staff.spec.ts`, `journey.spec.ts` and `workspace.spec.ts` initially passed 11 of 13 checks in 10.6m. The older academic journey still sought lesson reviews on the application detail route; it now explicitly opens Lesson reviews. The new workflow exposed a long history identifier overflowing on mobile, corrected with readable labels, wrapping, a wider applicant header and history placed after actions. Its next run exposed the encoded follow-up URL bug described above. After these fixes, both affected complete journeys passed on a fresh isolated database: `npx playwright test --config .local/staff.config.ts tests/e2e/staff.spec.ts tests/e2e/journey.spec.ts --grep 'complete application|staff reviews a real' --reporter=list --output=.local/staff-final-results` — **2/2 in 3.1m**, saved run status passed, no failed tests. No assertion/timeout was weakened. The other eleven passed checks cover all four role workspaces, EN/HI at five widths, keyboard/drawer focus, 200% CSS reflow, queue URL reloads, loading/error/retry, stale-write rejection, ownership and the existing public/adult flows. The whole unrelated browser suite and pixel baseline suite were not rerun for this extension.

Visual evidence is under `docs/visual-qa/staff/`: individually reviewed overview/network EN desktop and HI mobile, applicant interview, staff interview, Hindi mobile assessment, approved and terminated detail, and resolved follow-up. Final interview/approval/termination/follow-up captures were reopened after refinement. Sampled axe and overflow checks passed for captured workflow states; original queue heading order and mobile overflow were corrected. New screenshots show fictional test records only. No baseline was promoted; unrelated root-level captures were restored from pre-task copies. Other browsers/hardware and manual screen-reader checks remain unverified.

Installed `go run ./cmd/migrate -staff-only` on the configured development Atlas database: existing required fields are retained, optional interview validation and staff indexes added, and all **15 collections remain**. No new collection or database deletion occurred. The broader extension migration/capacity blocker remains. Built and restarted the main Go API; the frontend on `127.0.0.1:5173`, backend on `127.0.0.1:8080`, staff login, six staff read endpoints and logout passed against Atlas. Private `.env` remains unchanged at 2026-09-23 01:53:45. Temporary test services are stopped after verification and their actual databases retained. See `staff-operations.md`, updated permissions/state machines and acceptance matrix for feature boundaries. No public deployment, real meeting creation, external delivery or charge occurred.

## 2026-09-23: clear Parent, Tutor and Staff sign-in

The shared generic heading made the account type unclear. Sign-in now has a prominent Parent / Tutor / Staff switcher, a matching heading and icon, and a restrained glossy sage selected state. All text is bilingual. Parent is the default; Tutor is selected for application/availability entry points. Safe return paths survive account switching, reloads and sign-in/signup navigation. Staff has its own visible choice and no account-creation action; public signup still exposes only parent/tutor. Already authenticated users see their actual account type even if a different role is specified in the URL.

The switcher expresses sign-in intent, not an authorization input: login continues sending only email/password, and the Go session's stored role determines access and workspace routing. No backend, migration, provider, private environment or deployment changes were made. Existing illustration, colours, signup layout and the earlier removal of preview/site-return links are preserved. Implemented directly without the prohibited update-design skill.

Typecheck, lint, 3 component tests, production build, changed-file Prettier and `git diff --check` passed. Before editing, the current English desktop and Hindi mobile login images were opened. After editing, all six Parent/Tutor/Staff English desktop and Hindi mobile captures in `docs/visual-qa/login-roles` were individually reviewed; their axe scans, five-width reflow checks and page-error checks passed. The additional English mobile tutor capture was also opened. Only the two reviewed login baseline images were replaced; both unchanged signup baselines passed.

Selected Chromium regression used actual Go and an explicitly selected local MongoDB replica set on isolated ports/database: `npx playwright test --config .local/polish.config.ts tests/e2e/accounts.spec.ts tests/e2e/login.spec.ts tests/e2e/accounts-visual.spec.ts`. Thirteen checks passed in the initial 14-test run (3.0m): all four pixel baselines, English/Hindi parent signup and persisted login, tutor application signup, error/retry/help, real Parent/Tutor/Staff localhost/canonical sign-in, server-role identity, Origin/CSRF rejection, role switching/back/reload, safe returns and signup intent. The keyboard/reflow test sent Tab before the lazy form rendered; it now waits for the email field before testing first-tab and skip-link focus. Its targeted rerun passed **1/1 in 29.4s**, covering all six account routes at five widths and 200% CSS reflow (`--grep 'auth pages reflow' --reporter=list --output=.local/login-roles-reflow-results`). This was a test readiness correction; no UI assertion or timeout was weakened. The separate backend/security suites and full application regression were not rerun for this frontend change.

Unrelated root-level screenshots were restored from their pre-task copies. Temporary test services were stopped and their databases retained. The user's main frontend (`127.0.0.1:5173`) and Go backend (`127.0.0.1:8080`) remain running; both returned HTTP 200 after cleanup. The private `.env` was unchanged. Atlas capacity and pending extension migrations remain outside this UI checkpoint.

## 2026-09-23: luminous landing-page refinement

User requested a landing-page redesign with the same colours, a crystal-like shine, and no development-preview banner. Implemented directly in the repository without the prohibited update-design skill. The homepage now has a lightly glazed header, pale sage light/facet background, stronger hero typography, a white-framed photograph with a translucent caption, highlighted process/tutor surfaces, a glossy ink continuity panel, a contained FAQ, a compact two-column closing action and an ink footer. Hero copy is shorter in English/Hindi. The homepage preview strip, operator-placeholder footer sentence and component-library link are removed. Other public routes retain their existing presentation.

The existing generated photo and visible AI provenance label are preserved. Tutor discovery still queries Go/Mongo; sample identities, trial terms and actual launch limitations remain truthful. Login/signup, private workspaces, backend permissions, payment/provider configuration and production release gates are unchanged. No new dependencies, raster assets, database migrations, deployment or external services were introduced.

Verification: typecheck, lint, 3/3 component tests, production build and changed-code Prettier checks passed. The selected Chromium run passed **12/12 in 2.4m**, with no failed/skipped cases: `npx playwright test --config .local/polish.config.ts tests/e2e/home.spec.ts tests/e2e/visual.spec.ts tests/e2e/accounts-visual.spec.ts`. It checks English/Hindi at 360/390/768/1024/1440, image decoding, aligned equal desktop hero columns, no overflow, keyboard FAQ/menu/skip link, 200% CSS reflow, sampled axe, real profile/matching/login navigation, tutor loading/error/retry, three reviewed new homepage pixel baselines and seven unchanged profile/empty/account baselines. Only the three homepage baselines were replaced, each after opening the candidate. A separate actual-browser check passed for forced-colour heading visibility, signed-in footer axe and real sign-out; its first draft checked before logout completed, then passed after waiting for the footer to disappear. No application change or timeout increase was needed for that check.

Visual evidence: `docs/visual-qa/landing-crystal/`. Individually reviewed the four EN/HI hero desktop/mobile captures, complete English desktop composition, five desktop sections, three mobile sections and the signed-in mobile footer. Additional full-page images were saved but are not independently claimed reviewed. The previous desktop/mobile views were inspected before editing. Unrelated regenerated root screenshots are restored to their pre-task copies. Browser evidence is Windows Chromium; other browsers/hardware, screen readers and performance lab scores remain unverified. The backend was not changed, so Go tests and the wider private-workflow regression were not rerun.

Tests used an explicitly selected actual local MongoDB replica set and isolated Go/Vite ports. Local test data is retained and temporary test services are stopped after verification. The normal frontend/API remain on 5173/8080 with the existing Atlas configuration. Atlas collection capacity and pending extension migrations remain unresolved; this visual work does not certify production readiness.

## 2026-09-23: focused account and teacher workspace refinement

User direction: remove development-preview presentation from parent/tutor sign-in and workspaces, remove website-return navigation, reduce helper copy, retain the sidebar and paper/ink/sage palette, and improve the teacher layout with brighter surfaces. The user explicitly prohibited the update-design skill; this preference is recorded in AGENTS.md. Work continued directly in the project. No deployment was requested or performed.

Implemented: parent/tutor workspace preview strip removed; login/signup preview footer, public-site header links, repeated reassurance copy and marketing footer removed. Sidebar marketing panel and website-return link removed. The account illustration and working form controls remain. Private surfaces use restrained light highlights and shadows in the existing palette. The teacher overview now shows the earliest upcoming confirmed trial, three actual trial counts, up to three pending requests with a link to the complete returned queue, a compact saved application/scope panel, and tuition/update navigation. The dedicated Sessions view supports status filters in URL state with real empty results. Application feedback remains visible for non-approved applications. Hindi/English text is maintained.

Server authorization, academic approval, sessions/CSRF, production release gates and provider settings are unchanged. Sample identities and action-specific development/guardian/payment disclosures remain labelled. This visual change does not resolve the existing Atlas collection limit or install pending migrations.

Verification: `npm run typecheck`, `npm run lint`, `npm run test` (3/3), `npm run build`, changed-file Prettier checks and `git diff --check` passed. The selected Chromium regression passed **25/25 in 10.5m**, with no failures/skips: accounts, account visuals, localhost login/Origin/CSRF, workspace navigation/filters/error recovery/learner selection, workspace visuals, and the complete application → approval → trial → reviewed-progress journey. The journey now follows the confirmed-trial link and review filter after actual server-confirmed transitions. These checks used the real Go API and an explicitly selected local MongoDB replica set on isolated ports/database, via `npx playwright test --config .local/polish.config.ts tests/e2e/accounts.spec.ts tests/e2e/accounts-visual.spec.ts tests/e2e/login.spec.ts tests/e2e/workspace.spec.ts tests/e2e/workspace-visual.spec.ts tests/e2e/journey.spec.ts`. The temporary config reuses only the isolated test server. The separate Go/security suites and full 39-test regression were not rerun for this frontend change.

Visual evidence: fourteen images in `docs/visual-qa/polish` were individually opened, covering account/login, parent first-use, teacher empty/populated overview and session forms. The corrected Hindi subject captures were reopened. Six changed Windows pixel baselines were individually reviewed before promotion and all six passed. Tests cover five widths, English/Hindi, keyboard/drawer focus, CSS 200% reflow and sampled axe scans. No cross-browser/native-screen-reader certification is claimed. Unrelated screenshots regenerated by the regression were restored to their pre-task versions. The initial lint finding about reading the clock during render was fixed with state and a cleaned-up interval before final lint/build passed.

The main app remains running at `http://127.0.0.1:5173` with Go on port 8080; both returned HTTP 200 and the API reported ready. The private `.env` timestamp remains unchanged. Temporary test services are stopped after verification; their local test data is retained. No Atlas databases, migrations, live providers or deployment settings were changed.

## 2026-09-23: tuition, service, billing and security implementation checkpoint

The complete original master prompt and identical product specification were read before implementation. Existing approved account/public/private designs, Go/React/Atlas architecture, email/password override and no-Docker DigitalOcean target are preserved. The user reports an approved Razorpay account; no payment credentials, Route approval or live transaction is inferred.

Actual working additions:

- Ongoing tuition: weekly availability/leave/buffer/capacity, immutable agreements, family proposal/tutor acceptance, recurring reservations, paid holds/expiry, rescheduling/makeup, lesson evidence/review, versioned plans and family-consented handover. Tutor access ends after replacement or closed enrollment; shared trial/class guards prevent conflicts.
- Billing: real Go Razorpay HTTPS adapter, server-priced intents, verified/deduplicated webhook receipts, leased jobs/retries, capture/refund journals, separate refund review, order reconciliation and late-capture review. Runtime checkout remains disabled without genuine test configuration; no simulated provider success. Commission, tax invoices, disputes and payouts remain open.
- Service: parent-visible conversations, owner-only in-app notifications, support/matching/privacy requests and separately restricted safeguarding. Cases require eligible operator assignment before details are visible. No automatic privacy deletion or external escalation/delivery is claimed.
- Account: persisted name/language preferences, active sessions, owner-scoped revocation, current-password verification and atomic password/session rotation. Direct Go entrypoints now safely load development `.env` from the working directory or up to two parents, preserving explicit environment values and never evaluating/logging secrets. Production uses its explicit systemd environment.
- Files: private disk development and S3-compatible Go SDK adapters, MongoDB metadata/receipts/quota, type/signature/size checks, quarantine, leased ClamAV INSTREAM scans and guarded integrity-checked downloads. Files are connected to teaching applications and tuition. An absent/failing scanner never unlocks downloads. Actual S3 and ClamAV deployments remain unverified.
- Staff: audited provision-only Go CLI with hidden terminal/explicit secret-manager input; duplicate public accounts cannot be overwritten or promoted. Finance and support land on their actual billing/request work areas while academic APIs remain denied.

Executed backend/frontend evidence: final `go test -race -count=1 ./...` and `go vet ./...` passed with `TEST_MONGODB_URI` explicitly selecting the actual local WiredTiger replica set. This includes 51 API/database security subcases (22 original, 9 tuition, 8 billing, 4 cases, 3 account, 5 files), the staff provisioning database case and unit checks for password protection, recurrence/clock changes, environment parsing, provider wire/signatures and private storage/scanner protocol. Final frontend typecheck, lint, 3 component tests, production build, Go formatting and Prettier checks passed. OpenAPI generation succeeded. Actual Atlas verification of the new extension remains blocked by capacity.

Browser verification: the complete Chromium regression passed **39/39**, with zero unexpected, flaky or skipped tests (HTML report and saved run status verified, completed 12:13:43 IST). This includes all twelve reviewed pixel baselines, the original four-role journey, ongoing tuition/handover, conversation/notification, support resolution/reopen, file quarantine, password/session rotation, saved Hindi preference on a new browser context, learner selection and corrected finance/support landing routes. Earlier runs exposed shared-family/shared-availability test data and ambiguous global status locators, corrected with distinct fictional families, explicit saved-availability setup and learner-specific cards. The first signup timed out while build/race work competed for CPU; it passed in isolated and final runs. No timeout increase or blind baseline replacement was used to conceal these failures. After this aggregate pass, final visual review prompted a small admin overview refinement. Its affected rerun passed **2/2 in 3.5m** (`npx playwright test tests/e2e/journey.spec.ts tests/e2e/workspace.spec.ts --grep 'complete application|admin workspace'` with the explicit local test URI). A final browser recapture after removing a duplicated decorative arrow checked both languages at 1440/390, five recent decisions, one arrow, no overflow and no page errors; English desktop and Hindi mobile images were opened again.

Visual review: all 23 new module screenshots have been individually opened. Latest recaptures of calendar/availability/plan/handover, account Hindi, private files, billing, support, populated family/tutor/mentor views and admin English desktop/Hindi mobile were inspected. Both workspace baseline candidates were reviewed; only English desktop was promoted for the functional sidebar additions. Hindi mobile was byte-identical and the ten public/account baselines were unchanged. All twelve pixel checks passed in the full 39-test regression. Actual Chromium and real Go/Mongo were used; provider/scanner substitutes exist only inside backend tests. Sampled axe/keyboard/reflow checks are not accessibility certification. Details are in `visual-qa/README.md`.

Refinements: fixed Hindi tuition-tab overlap; invitation navigation lost during unmount; captured a settled calendar instead of its loader; moved core navigation before support/account links; prevented edits being cleared during an in-flight message send. Captured-payment behavior after a capacity reduction now rolls back all attempted promotions and records refund review; failed-refund retries release reserved balance once. Availability now uses local wall-clock boundaries across spring/fall transitions and rejects half-hour clock folds; the corresponding regressions passed. Availability transaction retries retain the original expected version. New audit events have English/Hindi labels. Admin overview shows five recent decisions with a direct history link rather than an unnecessarily tall timeline; the separate history view explicitly discloses its 25-record display limit. No production-readiness claim.

Infrastructure defect: Atlas reports **500 collections**. Read-only diagnostics found 27 retained automated-test databases containing 398 collections; `.local/atlas-test-databases.json` is the exact proposed cleanup manifest. The configured app database is excluded. Explicit cleanup confirmation is pending; **nothing has been deleted**. Private `.env` remains unchanged and points to Atlas. Local MongoDB is only an explicitly documented verification environment, never an application fallback. New source migrations have not been applied over the user's running Atlas app; capacity must be resolved first.

Remaining implementation: broader discovery filters/shortlist/comparison and operator matching; genuine tuition reviews; renewals; assessment appointments/reassessment and specialist conflicts; staff MFA/invites and password recovery/delivery; approved commission/earnings/invoices/disputes/payout integration; CMS/operational reports; public pre-rendering/performance; richer schemas/remaining legacy pagination; retention/deletion/restore and full release checks. These are unfinished implementation items, not all external credential blockers. `completion-plan.md` and the acceptance matrix track these boundaries.

Setup/live blockers: Atlas capacity; real Razorpay test keys/webhook and sandbox acceptance, separate approved payout arrangement; private storage/scan service hardening and verification; guardian-verification and legal/retention/merchant policies; actual staffing; GitHub remote/droplet/domain/TLS/backup restore; independent security/accessibility and operator release review. Production authentication/payments remain deliberately gated. No real delivery, charge, public deployment or Docker was introduced.

Operational handoff: security, architecture, data model, role permissions and DigitalOcean service documentation now describe the extension rather than the former first-slice boundary. `operator-walkthrough.md` provides a concrete review worksheet; it has not been signed off. CI's time budget accommodates the larger regression but GitHub CI itself has not run. `scripts/check-private-config.mjs` checked 184 text files against three private values, found no matches and confirms no private environment file is tracked; it is a limited check, not a general secret scan. `git diff --check` passed. Existing Atlas API `/api/v1/ready` still returned `ready`; this is not evidence that the new migration is installed there. The private `.env` timestamp remains 2026-09-23 01:53:45.

## 2026-09-23: distinct post-login learning workspace

The user requested an app-like dashboard distinct from the public website, with the approved colour theme retained. `/workspace`, `/match` and `/apply` now share a dedicated paper/ink/sage shell: dark role-specific navigation, compact utility bar, private-workspace footer, mobile drawer and deliberate English/Hindi layouts. Public landing and account design remain separate. All actions still call the Go API and supplied Atlas cluster; no backend, database or authentication architecture was replaced.

Actual working features: parent first-use onboarding, owned learner switching with selection preserved across views/reload, current learning focus, saved needs, sessions and reviewed evidence; tutor actual session counts and application stages; mentor assigned assessment/lesson-review views; admin tutor scopes and decision history. Staff can search/filter the returned application records, preserve filters on reload and clear real no-match results. Summary counts are derived from authorised records and explicitly bounded to records in view. Session loading/failure, request retry, actual sign-out, private return links and role denial are handled. Application and requirement multi-step forms inherit the new shell. Registration still never grants approval or public booking eligibility.

Verification: final `npm run e2e` passed **33/33 in 12.0m** against an isolated real Atlas database, including the complete application → assessment → scoped approval → learner/requirement → trial → lesson evidence → academic review → family progress journey. All 12 reviewed Windows pixel baselines passed; the existing 10 public/account baselines were unchanged. Seven new workspace tests cover four-role navigation, both languages at five widths, keyboard/drawer focus, 200% CSS reflow, default axe scans, real learner switching, queue filters, loading/error/retry, logout and role denial. Final typecheck, lint, 3 component tests, Prettier check and production build passed. Live API readiness returned HTTP 200 with `ready`. A source/untracked-file scan found no private URI or fixture password; `.env` remains ignored. Backend source is unchanged; the separate Go race/integration suite was not rerun for this frontend change.

Browser review/refinements: inspected populated parent progress, tutor lesson form, mentor scorecard and admin decisions in desktop/mobile layouts, plus first-use onboarding, drawer, loading/error and empty-filter states. Fixed low contrast on the first-use step numbers, ambiguous complementary landmarks, search input label association, mentor empty-state copy and the sidebar background below the initial viewport. Fast adjacent filter changes could overwrite the previous query during a pending React Router render; changes now merge with the current browser URL. The same refinement preserves the latest view when changing learners. Tests assert the combined URL and reload persistence. Two first-use parent candidates (English desktop/Hindi mobile) were individually opened before promotion. Existing public/account baselines are retained. The original full-journey test reached the final admin sign-in after completing family progress but exhausted its 90-second total budget on this machine; its total budget is now 180 seconds, with individual assertion timeouts unchanged. The rerun completed successfully.

Remaining scope/setup: this is the private dashboard redesign for the implemented journey. Enrollment/agreements, recurring scheduling, uploads/messaging, payments/ledger, broader privacy/operations and release work remain open in the acceptance matrix. Email ownership/recovery, staff MFA/provisioning, guardian verification, legal/provider approvals and operator release review remain live-launch blockers. Browser evidence is Windows Chromium; native zoom, manual screen readers, other browsers/mobile hardware and Lighthouse remain unverified. Start locally with `./scripts/api.ps1 api` and `npm run dev`; open `http://127.0.0.1:5173/workspace`. Existing private `.env` supplies Atlas configuration. No Docker, Vercel deployment, real payment or external delivery was introduced. GitHub remote and droplet/domain/TLS setup still require operator configuration.

Unresolved defects: no known blocking defect in the checked dashboard flows. The final long-name learner screenshot was reopened after the URL-state fix; it retains the correct learner view and wraps the full name. Unrelated public/account screenshots regenerated by tests were restored. This checkpoint does not establish production readiness or acceptance of the remaining platform scope.

## 2026-09-23: photo-led homepage and balanced landing layout

The user approved the account-page design and requested a warmer, more balanced homepage with hero imagery. The landing page now shares the account paper/ink/sage palette, with equal desktop hero columns, an original generated tutoring scene, consistent section edges, an evenly spaced support strip, three process steps, real tutor cards, a dark learning-continuity panel, keyboard-operable FAQs and a clear final call to action. Hindi and English have deliberate heading and spacing rules. The mobile layout stacks the content without horizontal overflow. Account pages retain their reviewed rendering.

Actual working features: existing Go/Atlas tutor discovery, profile links and requirement/sign-in entry points remain connected. No public approval rules changed. Homepage query loading and failure/retry remain real; retry restores actual API results. The error message now refers to tutor discovery instead of claiming the anonymous visitor has saved data. Tutor-card footers align when descriptions vary. The first complete multi-role development journey and email/password signup/login passed regression checks.

Image: generated using the built-in image tool, visually inspected, labelled as an AI-generated illustrative scene in English/Hindi, and saved as four local responsive WebP variants (45,686–164,492 bytes). Image sizing accounts for the cover crop and reserves layout space. No runtime image provider or extra dependency is needed. The exact prompt, provenance and final paths are in `hero-image-prompt.md` and `assets.md`. The people shown are not actual platform users.

Verification: full `npm run e2e` passed **24/24 in 2.8m** against an isolated real Atlas database, including signup/login, tutor application → assessment → scoped approval → requirement → trial → reviewed progress, real no-match search, and all 10 exact-pixel Windows baselines. Following the final error-copy/loading-tone/card-alignment refinements, the affected homepage/public/account visual suites passed **12/12 in 1.1m**. The two new homepage tests cover decoded images, equal desktop columns/section alignment, English/Hindi at 360/390/768/1024/1440, keyboard skip/FAQ/menu, 200% CSS reflow, actual profile/requirement entry links and failed-request recovery. Axe found no violations on the sampled homepage widths/languages. Final typecheck, lint, 3 component tests and production build passed. API `/api/v1/ready` returned `ready`. Backend source was unchanged; the separate Go integration/race suite was not rerun for this frontend change.

Screenshots opened: English desktop and English/Hindi mobile hero candidates (each opened before updating only the three homepage baselines), full homepage English/Hindi desktop/mobile, 1024px desktop composition, loading/error tutor sections, and English/Hindi 200% process viewports. Account/profile/no-match baselines passed without replacement. See `visual-qa/README.md`. Inspection prompted sharper desktop image selection and contextual error copy. Unrelated screenshots regenerated by the full journey were restored to their prior reviewed versions.

Unresolved defects: no known blocking defect in the checked homepage and existing journeys. Browser QA here is Windows Chromium; native browser zoom, assistive-technology review, Safari/Firefox, mobile hardware and Lighthouse/field performance remain unverified. Generated hero content still needs operator review before live use.

Remaining scope and setup: this checkpoint redesigns the landing page, not the unimplemented milestone C/D features. Enrollment/agreements, recurring scheduling, messaging/uploads, payments/ledger, broader operations/privacy and release checks remain open in the acceptance matrix. Email ownership/recovery, staff MFA, guardian verification, legal/operational policies and provider approvals remain live-launch gates. The supplied private Atlas `.env` is used by the running API; start commands remain `./scripts/api.ps1 api` and `npm run dev`, then open `http://127.0.0.1:5173`. No Docker or public deployment was introduced. GitHub remote, droplet/domain/TLS setup and operator deployment approval remain pending.

## 2026-09-23: email/password and account experience redesign

The user's latest instruction supersedes OTP authentication. `/signup` now creates actual parent/adult-learner or tutor accounts; `/login` uses email/password. Both are connected through Go to the supplied Atlas cluster. Public signup cannot create staff accounts or academically approve tutors. Staff sign-in accepts provisioned fixture email/password credentials. The old OTP endpoints and public identity/code selector were removed; legacy OTP sessions are rejected. Existing learning/application records were preserved.

Design: dedicated paper/ink/sage account shell, custom native notebook illustration, more readable fields, deliberate English/Hindi typography, parent/tutor role controls, password reveal, validation, pending states, preserved signup intent, and an explicit recovery-help dialog. Mobile prioritizes the form. Inspected desktop login/signup in English, desktop login in Hindi, mobile login/signup in both languages and staff Hindi mobile. Corrected a clipped illustration caption, excess signup whitespace and small field text. Four account baseline candidates were individually opened before promotion; no credentials were entered in those screenshots.

Backend: salted Argon2id (19 MiB/2 iterations/1 lane), normalized unique email credentials, transactional user/credential/session creation, bounded password work, persisted IP/email limits, generic login failures, opaque hashed sessions and unchanged role/CSRF controls. Migration and optional fixture credential seeding passed against `tutor_dev`. Root `.env` now uses `AUTH_PROVIDER=password`; a private random `SEED_PASSWORD` was added for existing fictional staff accounts. No secret values were displayed or committed. Old unused OTP secret configuration, if present, is not read. No real SMS, email or payment action occurs.

Verification: Go formatting/vet and `go test -race ./...` passed; `./scripts/integration.ps1 -Race` passed **22 Atlas subcases plus 5 unit tests** (including concurrent same-email signup across two API instances, session/privacy/approval boundaries and the original complete teaching journey). Full `npm run e2e` passed **22/22 in 3.0m**, including all 10 reviewed exact-pixel baselines. After refining account-load errors, supporting trailing-slash routes and correcting a screenshot wait, the affected account suites passed **12/12 in 1.2m**. Typecheck, lint (no warnings), 3 component tests, contract generation and production build passed. Browser checks cover English/Hindi parent signup, tutor signup, login/reload, error/retry, keyboard/reflow and negative Origin/CSRF; axe reported zero violations on examined account screens. A separate real-browser config failure/retry check passed and its loading/error images were opened. Live Atlas readiness returned ready. Source/untracked-file scans found no private URI, seed password or old OTP secret; `.env` remains ignored.

The initial migration of a Hindi test hit a shell-encoding issue in its locator strings; corrected the test text and reran successfully. One role screenshot initially caught the transient route loader; fixed the test to wait for the actual form before screenshot/axe review. No known unresolved blocker in the checked login/signup paths. The original master prompt and full spec copy remain unchanged; the user override is recorded separately.

Known remaining limitations: email ownership is not verified; password recovery/change, breached-password screening, staff MFA/provisioning and operator release review are pending. The help dialog does not pretend to send a recovery email. Production account access remains gated. The broader milestone C/D scope remains as listed below. This checkpoint redesigns account entry/onboarding; it does not claim every remaining platform screen has been redesigned or completed. Deployment target remains GitHub/DigitalOcean, without Docker; no public deployment occurred.

## 2026-09-23 follow-up: local login address failure

Reproduced the user's failing "Get development code" step: at `http://localhost:5173`, Go returned HTTP 403 `origin`; at the configured `http://127.0.0.1:5173`, challenge creation, code verification and dashboard access succeeded. API/Atlas health alone did not establish that the user's browser origin could sign in. The earlier all-green journey tests used only the canonical address and missed this case.

Vite now redirects local HTML navigation from `localhost` to its bound `127.0.0.1` address on the same port, preserving paths and query parameters. This runs only on the loopback HTTP development server. API requests are not redirected or given rewritten Origin headers; Go's origin and CSRF rules are unchanged. Existing pages must be reloaded. README now includes the command from `apps/api` and explains that the development code is shown on screen, not sent by SMS.

Added parent English desktop and mentor Hindi mobile browser regressions for the alias, query preservation, login and session persistence after reload, plus negative Origin/CSRF checks. The first targeted run caught a development-banner landmark issue; added a named region. Inspected `login-en-desktop.png` and `login-hi-mobile.png` before code generation, so tracked images contain no OTP.

Verification: `npm run typecheck`, `npm run lint`, `npm run build` passed; `npm run test` passed all 3 component tests; the final `npm run e2e` passed **13/13** in 2.4 minutes against an isolated Atlas database, including the 3 new login/security regressions and all 6 unchanged visual baselines. Login axe scans passed with no violations; successful login runs reported no browser errors. Also exercised the actual running app on port 5173 from `localhost`: redirect, challenge HTTP 201, login, refresh, and authenticated dashboard HTTP 200. Go code was unchanged; the separate Go race/integration suite was not rerun for this frontend tooling fix. No known unresolved defect in these checked login paths. Remaining feature scope and live-launch blockers are unchanged; no deployment or provider integration occurred.

## 2026-09-23 checkpoint: first persisted journey implemented

Full specification read and preserved. Repository initially contained only the master prompt; no ancestor/local AGENTS or application existed. User override recorded: **no Docker**; GitHub → DigitalOcean/systemd/Nginx + Atlas. User supplied Atlas credentials in a file named `,env`; corrected the filename to `.env` and verified it is ignored by Git. No URI/secrets are in source or docs. The Atlas connection, indexes and idempotent sample seeding succeeded in `tutor_dev`.

### Actual working features

- Bilingual design tokens/components and responsive editorial homepage, scoped tutor search with mobile drawer and real no-match state, tutor details, login, standards/approach/draft policy/support pages, and internal component showcase.
- Distinct parent, tutor, mentor and admin workspaces. Public browsing does not require signup.
- Real Go/Atlas journey: tutor submits private application → assigned mentor records six-dimensional assessment/evidence → approves a class/subject/mode scope → guardian-gated or adult-self learner and saved requirement → parent requests trial with versioned zero-fee development terms → tutor acceptance reserves both schedules transactionally → lesson evidence → assigned mentor review → family sees reviewed progress.
- Draft/back/refresh persistence, multiple learners, cross-household denial, staff role boundaries, suspension/removal from discovery and saved operations follow-up, explicit scope/session/OTP expiry, replay protection, logout/privilege revocation, CSRF/Origin controls and anonymous session status without spurious 401 requests.
- Actual Atlas repositories and transaction guards shared by all API instances; no in-memory application fallback or localStorage CRUD. Only language preference uses localStorage.

### Verification executed

| Command/check | Actual result |
|---|---|
| `./scripts/api.ps1 migrate` / `seed` | Passed against supplied Atlas, idempotent migration/seed |
| `go fmt ./...`, `go vet ./...` | Passed |
| `go test ./...` | Unit tests passed; integration explicitly skips without TEST_MONGODB_URI |
| `go test -race ./...` | Passed for non-integration suite |
| `./scripts/integration.ps1 -Race` | Passed: 18 Atlas integration subcases plus 3 unit tests, including concurrent overlapping HTTP accepts from separate API instances and actual API stop/new client restart persistence. Latest integration duration 21.35s; no races reported. |
| `npm run typecheck`, `npm run lint` | Passed |
| `npm run test` | Passed: 3 Vitest/RTL component tests |
| `npm run build` | Passed: strict TypeScript + Vite production build; split account routes and bundled local font files |
| OpenAPI generation/type alignment | `node scripts/contract.mjs` and `npm run contract` passed; generated types used by frontend |
| npm install/audit | Locked exact versions; install reported zero vulnerabilities |
| Final running-app check | `/api/v1/ready` returned ready against Atlas. Chromium visited home, tutor search, tutor profile and login: zero browser console/page errors. Staged files passed a check for the private URI and OTP secret; `.env` is excluded. |
| `npm run e2e` | **10/10 passed** in the final complete rerun (1.7m): full four-role journey, bilingual/responsive public routes, loading/error/retry/keyboard/200% reflow, Hindi adult requirement, and 6 visually reviewed exact-pixel baselines. Axe scans on exercised screens reported no violations. |

Initial failed checks were fixed rather than suppressed: missing Vitest imports, form accessible label/hint mix-up, insufficient label contrast, 200% header overflow and mobile intrinsic grid width. The Hindi added test now waits for the backend-confirmed next step before refreshing, as the English persisted-draft test already did.

### Screenshots inspected

Actual screenshots were opened for homepage desktop/mobile in both languages, profile, no-match/filter drawer, parent reviewed progress, tutor class evidence, mentor scorecards and admin queues (including Hindi mobile), Hindi requirement form and 200% reflow. Six reviewed public-page baseline images are retained. Exact inspected files, fixes and limitations: [visual QA index](visual-qa/README.md). Homepage overflow assertions cover 360/390/768/1024/1440px. Keyboard skip/dialog focus and axe checks ran; this is not WCAG certification. Additional captured widths are not claimed individually reviewed.

### Remaining defects / scope

No known blocking defect in the tested first development journey. The expanded Hindi flow passed after correcting the test's save-confirmation wait. This is **not the complete platform**. Milestone A has the foundation and core exemplars, with additional catalog components pending. Milestone B has the bounded development journey; no payment provider is configured, so no payment success is simulated. Milestones C/D remain substantial: tuition agreements/enrollment, live phone onboarding/staff MFA, availability/recurrence/travel/holds, messaging/private uploads, versioned learning plans/handover, support/safeguarding/CMS/privacy operations, payments/ledger/refunds/outbox, broad regression coverage, pre-render/SEO, Lighthouse and operator release review. See the acceptance matrix for each boundary.

Production private endpoints deliberately return 503 and startup rejects demo authentication. Guardian declaration is development-only; it is never described as verification. Assessment scheduling currently records a development session instant, not a live appointment calendar. Queries cap at 100; cursor pagination and richer migration/schema validation remain open. Drafts save at successful step transitions, not on every keystroke. Private authored text stays in its original language.

### Setup and launch blockers

Run API with `./scripts/api.ps1 api`, frontend with `npm run dev`, then open `http://127.0.0.1:5173`. `.env` already contains the user-supplied Atlas configuration. Exact Windows/Linux commands are in README.md. Integration/E2E create isolated `tutor_test_*`/`tutor_e2e_*` databases and retain them for operator review; no existing database is dropped. The optional local MongoDB process was stopped once Atlas became available.

GitHub remote, droplet/domain/TLS and deployment authorization have not been supplied. Nginx/systemd/build/CI templates are present but not executed on GitHub or DigitalOcean. No public deployment, real SMS, charge, payout, account provisioning or paid resource creation occurred. Live OTP/provider approval, guardian verification, staff MFA, merchant arrangement, prices/policies/legal review, safeguarding staffing, production data review, private file security, backups/restore rehearsal and operator sign-off remain blockers; see release-checklist.md. No Lighthouse score, field performance, native-zoom or screen-reader certification is claimed.

## 2026-09-25: operator-approved Atlas test database cleanup

After the droplet migration failed with AtlasError code 8000, a read-only inventory confirmed 500 collections. The user explicitly approved deletion of the 27 reviewed automated-test databases listed in `.local/atlas-test-databases.json`. An exact-name allowlist and pre-deletion count checks constrained the operation; the configured application database was excluded. All 27 databases (398 collections) were deleted. Post-deletion inventory confirmed 102 remaining collections and unchanged collection names/counts in all eight preserved application databases, including `tutor_dev`. The local audit is `.local/atlas-approved-cleanup-20260925.json`. Earlier notes describing this specific cleanup as pending approval are superseded. The droplet migration must still be rerun; this cleanup does not establish deployment or production readiness.
