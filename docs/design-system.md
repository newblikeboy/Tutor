# Design foundation

GyanSetu homepage update (2026-09-25): the visible name is GyanSetu across public, account and private screens; the domain remains thegyansetu.in. The hero uses the operator-provided two-sentence headline and supporting copy, with a wider text column, a simpler rounded photo frame, home/online labels and a quiet glass caption. The primary action is "Find My Child’s Tutor" and the secondary action is "Apply as a Tutor"; they use the existing matching and application routes. Mobile has full-width stacked actions, readable copy and reserved image space. The paper/ink/sage palette, labelled AI illustration, real tutor discovery, keyboard controls and account layouts remain. Older notes below describe historical designs.

Tutor application compact form (2026-09-24): the existing workspace palette now carries a single full-width form with horizontal numbered tabs and a native step-position progress bar. There is no inner sidebar or duplicate step heading. Desktop fields use two columns; subject checkbox groups use three. Mobile shows six numbered tab targets with accessible full labels and the current section heading. Card padding is 16?24px, control targets remain at least 44px, and actions follow content without overlapping fields. Education has explicit qualifications, experience/work and optional-document groups. Read-only email, blank unset positive-number inputs and bilingual character-limit hints clarify field states.

Landing editorial update (2026-09-24): the same paper/ink/sage palette now uses an asymmetric hero, oversized Manrope with a Georgia italic accent (Devanagari retains its native font), a double-line arched photo frame, luminous CSS crystal forms and quiet glass captions. Numbered steps have original decorative notebook/matching/growth illustrations. Tutor cards remain database-backed, beside a compact editorial heading; a deep green continuity section uses an explicitly illustrative layered record. A line-based FAQ and crystal-accent closing panel complete the page. Copy is short in English. The homepage-only stylesheet preserves account/private screens, 44px actions, clear focus, reduced-motion and forced-colours fallbacks. No preview banner, statistics or testimonials were added.

Parent/tutor task refinement (2026-09-24): common labels are Home, Trial lessons, Regular classes, Help and Account. Account tabs separate Profile, Password and Sign-ins; the form width is capped at 860px. Class sections use the shared keyboard-accessible tabs and retain unfinished forms after visiting a section. Only active message/file/tutor-change queries refresh. A tutor reviews schedule/fees before accepting, and pause/end controls live beside those terms. Avoid repeated explanatory introductions and duplicate shortcuts to the same next task.

Application drafts use one seven-section navigator: a side list on desktop and a native select below 760px. Save/continue remain reachable at the bottom of the viewport, with scroll clearance for focused fields. The four-stage approval tracker appears after submission. Status, saved application and documents are separate tabs. Teaching availability uses native date selection, removable dates off and duplicate prevention; a selected date is included by Save even if Add date was not pressed. Keep the existing crystal paper/ink/sage treatment, readable form widths, 44px controls and concise bilingual labels. Do not remove meaningful consent, payment or scan restrictions when reducing copy.

Parent simplification (2026-09-24): a compact first-use card has one primary action and three short step labels. Home prioritises one next action/trial; Learners owns saved learning needs. Trial lessons has Upcoming, Feedback and Cancelled tabs using the shared keyboard/URL pattern, while Regular classes remains the recurring-tuition destination. Only multi-learner families see a switcher. Matching uses four visible stages, one form column, short bilingual labels and inline validation. Repeated help, marketing-style empty cards and duplicate requirement summaries are omitted. Consent and contractual terms remain visible where decisions require them.

Staff tabs (2026-09-24): related queues share a sidebar entry, with URL-controlled tabs for overview queues, applications/interviews, tutors/follow-ups, applicant detail and staff payment records/operations. Shared `workspace-tabs.tsx` uses named tablists/panels, manual keyboard activation, 44px targets and wrapping English/Hindi labels. White/sage reflective surfaces retain the existing palette. Inactive read panels unmount; visited review forms preserve inputs and disable background member queries when hidden. No update-design skill is used.

Staff operations (2026-09-23): the private sidebar separates applications, interviews, tutor network, follow-ups and decision history. A compact overview uses real server aggregate counts. Search/status/reviewer filters persist in URLs; queues and history load additional pages. Applicant details pair application/interview/history with an action column, collapsing to one column on small screens. Sage highlights and white reflective surfaces preserve the approved palette. Actions follow the actual role, assignment and state; destructive actions have a named confirmation, reason and explicit consequences. Hindi and English labels, keyboard focus, retry/empty states and downloaded calendar entries are supported. Staff aggregate counts supersede the earlier loaded-record convention for these new endpoints.

Sign-in identity (2026-09-23): `/login` now exposes Parent, Tutor and Staff as an accessible three-link account switcher above a matching role heading and icon. The selected link has a pale, glossy sage surface, a border and `aria-current`; keyboard focus remains explicit. `/login?role=parent`, `/login?role=tutor` and the existing `/login?staff=1` can be bookmarked. Tutor intent is inferred from application/availability return routes and retained between sign-in and signup. Staff sign-in has no registration action; public signup remains parent/tutor only. The account returned by Go still controls permissions, landing behavior and the heading shown to an already authenticated user. No preview strip, website-return link or extra instructional copy was added. The existing illustration, signup composition and paper/ink/sage colours are retained.

Landing-page crystal finish (2026-09-23, latest): `styles/home.css` keeps paper #F5F3ED, ink #15353B and sage #DFE8BC, adding pale white surfaces, inset light edges, restrained shadows and static faceted light. A glazed header aligns with the hero; the existing photo receives a white frame and readable translucent caption. Process/tutor cards, the ink continuity panel, FAQ and a compact two-column closing section share these treatments. Mobile stacks naturally. The heading's gradient has an explicit forced-colours fallback, and motion remains limited/respects the existing reduced-motion rule. Only `.landing-shell`/`.home-*` styling is affected. The development banner is omitted only on the homepage in the public shell; AI/sample disclosures and server release gates remain.

Account/workspace refinement (2026-09-23, latest user direction): parent and tutor shells omit the development-preview strip and marketing/sidebar website-return links. Sign-in retains the approved illustration and form design, with less supporting copy and no public-site navigation. Private surfaces keep paper/ink/sage, adding restrained highlights, white surfaces and subtle shadows. The teacher overview prioritises the next actual confirmed trial, compact record counts, pending requests and a concise application panel. Trial status filters use URL state; recurring tuition remains accessible through its existing calendar. Sample-record labels, action-specific development disclosures and backend release gates are preserved. Changes are made directly in the repository; the user prohibited the update-design skill.

Warm academic editorial direction. Brand wordmark: GyanSetu (user override, 2026-09-25); domain: thegyansetu.in. Preserve exact spelling and casing, the current paper/ink/sage palette and book icon. No invented founder, statistics or endorsements. Label illustrative imagery; never present generated people as actual tutors or learners.

Tokens: navy #13213C, teal #087F8C (large elements), darker teal #076B76 for normal text/buttons; paper #F8F7F4; white surfaces; text #152238; secondary #546172; border #E2E6EB. Amber is a supporting surface, with dark text. Status always includes words/icons.

Self-hosted OFL Manrope Latin; body 16px/1.65 and large editorial headings. 8px spacing with 4px subdivisions; 12/16px radii; subtle shadows; 180ms interaction transitions; reduced-motion override. Public width 1248px.

Homepage (updated 2026-09-23): an equal-column editorial/photo hero at 1024px and above, stacked below; consistent 1248px section edges and 20px mobile gutters. It follows the approved account palette: paper #F5F3ED, deep ink #15353B, sage #DFE8BC and readable green #57744C. `styles/home.css` scopes these tokens to the landing shell, preserving account and workspace rendering. Three evenly spaced support pillars and numbered process cards lead to actual Go-backed tutor listings. A dark continuity section pairs explanatory text with a clearly illustrative family learning record; a native details/summary FAQ and centred sage call to action finish the page. The generated photograph, provenance and responsive asset choices are documented in assets.md. Search: filter rail/drawer, data-backed cards. Profile: scoped evidence and sticky request summary. Parent: learner focus and next action. Tutor: assigned work and lesson tasks. Mentor/admin: contextual queues and reasoned decisions, no decorative metrics.

44px target product standard, visible focus, skip link, semantic headings, labelled fields, native date/time fields, Radix dialog focus trap/restore, live announcements. 360/390/768/1024/1440 responsive targets. No persisted UI-language preference; clear the legacy language key at startup.

`/components` demonstrates the implemented component states in both languages. Browser artifacts and inspected status are recorded in progress.md; an image existing alone is not acceptance.

Private workspace (2026-09-23): `/workspace`, `/match` and `/apply` use a distinct application shell, with a 236px ink sidebar, compact utility bar, restrained headings and paper work surfaces. At 1000px and below the rail becomes a keyboard-operable modal drawer. `styles/workspace.css` scopes the shared account colours to this shell. The public header/footer are replaced by role navigation and a quiet private-workspace footer. Login/signup and the public landing retain their own layouts.

Parents have a deliberate first-use invitation, learner switcher, current focus, sessions, saved needs and reviewed progress. Tutors have actual session counts, teaching tasks and application stages reflecting saved status. Mentors have assigned assessment/review views; admins have tutor scopes and decision history. Search/status filters apply only to records returned by the authorised API, with explicit no-match states; counts describe the current returned records rather than global network totals. URL state preserves selected learners, views and filters across reloads. Loading, session failure, retry, sign-out, access denial and real empty records remain explicit. No placeholder notifications, earnings, invented charts or fake records are added.

Windows Chromium review includes English/Hindi desktop/mobile, five responsive widths, keyboard skip and drawer focus, default axe rules, CSS 200% reflow, and the real application/approval/trial/progress forms. Two individually inspected first-use parent baselines protect the private shell without depending on changing audit dates. Other role screenshots are review evidence, not deterministic pixel comparisons.

Account redesign (user request, 2026-09-23): `/login`, `/signup`, and staff sign-in use a dedicated editorial shell in `styles/auth.css`. Paper #F5F3ED, deep ink #15353B, muted sage #DFE8BC; readable 16px form text and 14px labels. The desktop spread pairs a custom fraction/notebook illustration with a focused form. Mobile puts the form first and reduces the illustration to a short supporting panel. Shared patterns include account-role radios, labelled password reveal, inline validation, pending submit, honest recovery help, and preserved return intent. Parent/tutor signup shares the design; staff use assigned email credentials with no public staff registration. No new raster assets, fabricated statistics, testimonials or third-party brand assets were introduced. Four account baselines were opened before promotion.
