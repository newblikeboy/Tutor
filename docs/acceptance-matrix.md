# Requirement → implementation → acceptance evidence

Encrypted one-way Updates (2026-09-26): admins send to activated parent/tutor inboxes; approved tutors send to currently assigned tuition parents; recipients cannot reply. Browser-encrypted subjects/messages, private-key vaults, signed recipient read receipts, unread counts, paginated Inbox/Sent and separate Activity screens are implemented. Full local-Mongo Go race suite/vet, eight frontend unit/component tests, strict TypeScript/lint/build, inbox + tuition/billing browser flows, support/Activity regression and both reviewed workspace baselines passed. Lost-response retry, tamper rejection without receipt, applicant reception, role/household/assignment denial, passphrase recovery and cache isolation are covered. Ten inbox screenshots reviewed with axe and responsive/keyboard checks. Additive `--inbox-only` migration is included in the operator deployment script. Live deployment, real-account activation and independent crypto audit remain unverified; passphrase loss has no recovery/reset, and this version has no forward secrecy. See [design and limits](encrypted-inbox.md), [progress](progress.md) and [visual evidence](visual-qa/encrypted-inbox/README.md).

Mobile public menu (2026-09-25): signed-out visitors have a direct Sign in link; signed-in visitors retain My workspace. Build/lint and a real local browser navigation/axe check passed; mobile/desktop captures inspected. Live deployment remains outstanding. See [progress](progress.md).

GyanSetu branding/homepage (2026-09-25): visible brand is GyanSetu; domain remains thegyansetu.in. Requested hero copy, parent matching and tutor application actions are implemented in the responsive paper/ink/sage design. Actual discovery and account layouts are preserved. Build/strict TypeScript, lint, three component tests, Go config tests/vet, three homepage browser checks and eleven reviewed visual comparisons passed. Desktop/mobile, private branding, keyboard/reflow and homepage axe evidence are recorded in [visual review](visual-qa/gyansetu-home/README.md). Local fixtures only; live deployment remains outstanding.

Custom domain (2026-09-25): operator configured `thegyansetu.in` and `www` on the droplet. External checks confirm valid HTTPS for both names, canonical redirects to `https://thegyansetu.in/`, branded HTML, HTTP 200 health/readiness and production configuration with authentication enabled. Certificate expires 2026-12-24; Certbot reports scheduled renewal. Account sign-in and renewal dry run were not tested, and the prior staff binary deployment concern remains unverified. See [progress](progress.md).

Owner-authorized reset (2026-09-25): confirmed live `tutor_dev` data reset transactionally, replacing 13 accounts with one administrator, parent and tutor. Three Cloudinary application assets deleted and zero remain in the app upload prefixes. Parent/tutor live HTTPS authentication passed. Administrator provisioning succeeded, but live login remains blocked by the old deployed API (`staff_auth_unavailable`); deployment of the existing GitHub release is pending. See [progress](progress.md).

Password minimum override (2026-09-25): signup, account password changes and provisioned staff now accept 8-128 characters. Existing passwords remain valid. Targeted Go race/vet, web build/lint/three component tests and two account browser regressions passed using local MongoDB. Seven-character rejection and exact eight-character passwords are covered, including all four staff roles. Four desktop/mobile captures inspected; signup baselines reviewed before replacement. No migration or environment change required. See [progress](progress.md).

Password-only staff override (2026-09-25): explicit user selection replaces the staff MFA prerequisite. Provisioned non-sample admin/mentor/support/finance accounts now sign in with email/password in production; public signup remains parent/tutor only. Full Go race/vet, frontend build/lint/three component tests passed. Real production TLS API tests and the final HTTPS browser run cover all four staff roles, persistence/logout, public staff-signup denial and support/finance academic denials. Two staff screenshots inspected; four public account captures match prior reviewed files exactly. Tests used only local MongoDB. Operator deployment and real staff provisioning remain console steps; no MFA claim. See [progress](progress.md).

Production account access (2026-09-25): explicit `AUTH_PROVIDER=password` now enables non-sample parent/tutor signup/login while retaining HTTPS, Secure cookies, Origin/CSRF, ownership and approval checks. Production sessions cannot replay development sessions; sample/staff accounts stay blocked and Nginx client IP limits are scoped to its loopback proxy. Full Go race/vet and web build/lint passed; real production-mode HTTPS browser signup/login/logout/reload and cookie checks passed for both roles. Six desktop/mobile screenshots were individually reviewed with axe/reflow checks. Staff MFA, minor guardian verification, email ownership/recovery and live payments remain outside this release. Actual droplet verification follows deployment. See [production-auth.md](production-auth.md).

Direct Cloudinary media (2026-09-25): user override implemented for browser-to-provider signed uploads, reference-only MongoDB records, provider confirmation, scoped expiring delivery links, image/PDF viewing and progressive MP4 playback. Cloudinary no longer depends on ClamAV. Full Go race/vet, web build/lint/3 unit checks, direct browser flow and legacy file regression passed; desktop/mobile captures reviewed. Live read-only private PDF lookup/delivery returned 200. New real-account uploads and droplet-origin playback remain unverified; auth/payment production gates remain. See [progress](progress.md) and [media architecture](private-files.md).


TheGyanSetu rebrand (2026-09-24): the user-selected name replaces the provisional wordmark across public/auth/private screens, browser metadata, checkout/calendar branding, API defaults and operational documentation. Existing book icon adapted as favicon; local API name verified. Build/strict TypeScript, lint, Go config tests/API compilation and all 11 reviewed visual comparisons pass (2.3m). Fourteen desktop/mobile captures inspected; additional title/drawer/favicon smoke checks pass. See [progress](progress.md).

Direct private-route sign-in (2026-09-24): the intermediate privacy invitation is removed. Signed-out visitors go directly to login, preserving their requested path/query through login and signup navigation. Loading/retry and server access gates remain intact. Build/strict TypeScript, lint and three focused browser cases pass across the initial run and corrected rerun; desktop/mobile screenshots inspected. See [progress](progress.md).

English-only interface (2026-09-24): language controls and Hindi UI resources/styles/font removed from public and private screens. Legacy browser and account display preferences cannot enable Hindi; profile saves persist English. Authored records and teaching-language/subject choices are preserved. Build/strict TypeScript, lint, 3 component tests and 30 selected Chromium cases pass across the main run and focused rerun. Eleven new Windows baselines were individually reviewed and compare exactly. See the latest [progress checkpoint](progress.md) for results and limits.

Teaching approach simplification (2026-09-24): demo choice/recording controls and sample worksheet removed from step 5 and editable review. Existing interview flow allows old incomplete recorded drafts to submit; private files remain retained. Build/strict TypeScript/lint and the focused legacy-draft browser regression pass (1/1, 52.6s); English desktop and Hindi mobile captures inspected. See [progress](progress.md).

Compact tutor application (2026-09-24): full-width six-tab form with step-position progress bar, one visible panel, compact grouped fields and a complete bilingual field/copy review. Drafts, uploads, conflicts, submission and approval gates retain real Go/Mongo behavior. Build/strict TypeScript, lint, 3 component tests and seven focused Chromium cases pass across the final run and corrected Hindi-selector rerun. Desktop/mobile screenshots and the field audit are in [visual review](visual-qa/application-compact/README.md); exact results and limits are in [progress](progress.md).

Staff-confirmed fees (2026-09-24): six-step tutor application and Teaching hours no longer accept tutor prices. Staff finalize Online hourly and Home weekly/monthly plans, including Home class count/duration, before approval. Fees are versioned, audited and persisted; parents see prices for approved scope. Online agreement pricing is server-calculated, rejects stale quotes and preserves old agreements. Real-Mongo full Go race/vet, contract/TypeScript/lint/build and 3 component tests pass. The selected Chromium run passed 10/10 in 9.3m, including fee persistence, parent price display, approval, uploads, and tuition. Eight final desktop/mobile images were inspected; details and focused changed-price evidence are in [progress](progress.md). Home plan pricing is ready for review; the existing Online Mathematics activation/booking boundary remains.

Application upload provisions (2026-09-24): About you includes an optional passport-size JPG/PNG photo (3 MiB), alongside the existing resume and up to six educational documents in Education. All use configured private Cloudinary document storage and immediately persist their application links. Photo links enforce image type, ownership, target and scan state on draft/submission; staff see the photo in the summary and Documents tab. Web build/strict TypeScript/lint/3 component tests, focused real-Mongo application/document race tests, media/provider race tests and Go vet passed. Final Chromium run passed **3/3 in 2.8m**: seven-step submission/staff review, recorded-demo persistence, and all requested uploads with refresh/submission/private access checks, Hindi axe and five-width photo reflow. Desktop/mobile captures were inspected under `visual-qa/application-uploads/`. Main frontend/API are running; the absent scanner continues to lock downloads. See [progress](progress.md) and [private files](private-files.md).

Landing editorial design (2026-09-24): stronger display typography, arched labelled image, CSS crystal highlights, illustrated steps, revised tutor/continuity/FAQ/closing layouts and shorter bilingual copy retain the paper/ink/sage direction. Real discovery, routes, sample disclosures and release gates are preserved. Build/strict TypeScript/lint/3 component tests and **13 selected Chromium checks** passed, including five-width bilingual axe/reflow, keyboard, reduced motion/high contrast, tutor-request recovery, 3 reviewed new homepage baselines and 7 unchanged public/account baselines. See [progress](progress.md), [visual review](visual-qa/README.md) and `visual-qa/landing-editorial/`. Backend/configuration unchanged; no deployment.

Application documents (2026-09-24): résumé plus up to six education PDFs/JPGs/PNGs now persist their selections immediately. All configured document uploads (including worksheets and tuition files) use authenticated Cloudinary raw storage; application videos use authenticated video storage. Server checks ownership/type/status on draft and submission. Full local-Mongo Go race suite/vet, web typecheck/lint/build/3 component tests and six distinct selected browser flows passed across the initial run and corrected PNG-fixture rerun. Live synthetic PDF/JPG/PNG uploads and exact-byte reads passed; unsigned access returned 401. Main providers are enabled; actual scanning remains unconfigured and downloads/playback stay gated. See [progress](progress.md), [private files](private-files.md) and `visual-qa/application-documents/`.

Parent/tutor simplicity (2026-09-24): common short bilingual labels, compact application navigation, account/class/application tabs, retained unfinished forms, deferred inactive queries, native saved days off, fewer duplicate actions and improved mobile spacing. Existing server approval, consent, scheduling, payment and scan rules are preserved. Build/strict TypeScript/lint/3 component tests and **17 selected browser cases** passed across focused runs and corrected reruns. Evidence and limitations: [progress](progress.md), [visual review](visual-qa/README.md), `visual-qa/product-ux/`. No API/schema changes or automatic baseline replacements.

Parent experience update (2026-09-24): shorter bilingual navigation, one home next action, trial/feedback/history tabs and fewer repeated panels. Selected learners carry into matching, Add learner starts fresh, and request/trial URLs resume saved records after reload. Consent, ownership and server-reviewed feedback boundaries are retained. Build/typecheck/lint/3 component tests and **seven selected browser cases** passed across the focused run and targeted reruns; exact results are in [progress](progress.md). Evidence: `visual-qa/parent-ux/`. No backend/schema changes or old baseline replacements.

Staff presentation update (2026-09-24): grouped sidebar entries and URL-controlled tabs show one overview queue, applicant section or staff payment list at a time. Inactive read panels defer data loading, while review forms retain unfinished inputs. Existing role gates, decisions and deep links remain supported. Typecheck/build/lint/3 unit checks and **6/6 selected Chromium cases** pass, covering request counts, navigation, input retention, permissions, staff lifecycle and stale decisions. English/Hindi axe/reflow and inspected captures are recorded in the latest [progress checkpoint](progress.md).

Recruitment integrations (2026-09-24): new tutors see application stages first; server-approved, unexpired scope unlocks teaching routes/APIs. Authenticated Cloudinary MP4 uploads persist bounded metadata in the application and require a real scan before private range playback. Staff schedule/reschedule/cancel through a durable Zoom OAuth worker, with host-conflict checks and duplicate-create recovery. Full local-Mongo Go race suite/vet, frontend build/typecheck/lint/3 unit tests and 3/3 selected browser flows passed. The additive application migration is installed on the existing Atlas collections. Cloudinary read-only authentication passed; Zoom OAuth and the configured host's meeting-list request both return 200 with all five required scopes granted. Main scanning is unconfigured; live uploads/meetings were not exercised. Exact evidence and setup: [progress](progress.md), [recruitment integrations](recruitment-integrations.md).

Final-submit 422 correction (2026-09-23): Home Tuition service localities ignore blank lines and surrounding whitespace before Go validation/persistence, while retaining required/count/length limits. Accessible bilingual field feedback and step validation are wired to the textarea. Nine boundary cases, existing validation and real-Mongo application integration pass under the race detector; focused Chromium error/recovery/persistence/submission test passes (1/1), with desktop/mobile axe/overflow checks and inspected captures. Web build/typecheck/lint and Go app vet pass. Existing main draft was inspected read-only and passes corrected validation; updated frontend/API are running.

Latest tutor application checkpoint (2026-09-23): seven sections with server-persisted drafts; structured Home Tuition/Online/both requests, travel/online readiness, IST availability, education/experience, demos, separate fee expectations and declarations. Tutor-owned API and form revisions prevent cross-user/stale writes; staff can review all private fields and filter requested mode. Employment eligibility is an administrator decision and approval remains server-scoped. Full real-Mongo race suite/vet and web unit/lint/build passed. Three new application browser cases passed (full flow/persistence/private staff review; stale drafts/conditional modes/Hindi mobile; quarantined MP4 with separate form revision). The 20 selected regressions passed across the initial 19/20 run and corrected literal-message file-test rerun (1/1). Main Atlas additive validation/index migration retains all 15 collections. Configured uploads/scanning and wider activation scope remain explicit limits; see [application-form.md](application-form.md) and the detailed progress checkpoint.

Current checkpoint: 2026-09-23. This matrix distinguishes implemented development behavior, executed checks, and remaining scope. It does not certify production readiness. The complete master specification was read before extension. User overrides: Go/React/Atlas, no Docker, GitHub/DigitalOcean, email/password instead of OTP, preserve approved designs.

Latest staff checkpoint: application/reviewer queues, guarded Zoom appointments, assessments and scoped approval, changes/decline, suspension/reinstatement/termination/reassessment, internal history and durable follow-up resolution are implemented. Full Go race suite/vet and final focused encoded-URL regression passed; frontend typecheck/lint/3 tests/build/format passed. Eleven selected browser checks passed initially; both corrected complete journeys passed their final fresh-database rerun (2/2, 3.1m). Desktop/mobile captures were inspected. The additive staff-only migration is installed on the configured Atlas app with all 15 collections retained; main frontend/API and staff endpoints are running. Automatic Zoom creation/external invitations and the wider previously pending extension migration remain outside this checkpoint. Details and earlier failures are recorded in progress.md.

Latest sign-in clarity checkpoint: visible Parent / Tutor / Staff navigation with a selected state, matching bilingual headings/icons and persistent URL intent. Staff registration remains unavailable, and authenticated identity/permissions continue to come from Go. Six desktop/mobile role captures plus English mobile Tutor were individually inspected; sampled axe, five-width reflow and page-error checks passed. Typecheck/lint/3 component tests/build passed. Thirteen initial browser checks passed, including all four account baselines and real login for all three account types; the remaining keyboard check passed its targeted rerun after fixing test readiness. Exact evidence is recorded in progress.md.

Latest landing-page checkpoint: same paper/ink/sage colours with crystal-like highlights and a refined hero/header/cards/FAQ/closing section; homepage development banner and operator-placeholder footer copy removed. Actual Go-backed discovery, AI image label and sample-record disclosures remain. Selected homepage/public/account Chromium checks passed 12/12, including three individually reviewed new homepage baselines and seven unchanged baselines; typecheck/lint/3 component tests/build passed. Desktop/mobile visual review and its limits are recorded in `visual-qa/README.md` and progress.md.

Latest UI override: no development-preview banner in parent/tutor account or workspace screens; no website-return navigation in account/private screens; concise copy, retained sidebar/palette and a reorganised teacher overview. Trial filters/counts remain based on authorised Go API records. Backend release gates and truthful labels on sample records and development actions remain in force. The user prohibited the update-design skill.

Latest refinement evidence: 25/25 selected Chromium tests passed against the actual Go API/local replica set, including account/login, six individually reviewed pixel baselines, four-role responsive navigation, tutor filter reloads and complete academic/trial/progress persistence. Typecheck, lint, 3 component tests and build passed. Fourteen screenshots were individually inspected; exact files and limitations are in `visual-qa/README.md`. This is a frontend checkpoint; it does not replace the earlier backend evidence or resolve remaining launch requirements.

| Requirement | Working implementation | Executed evidence / remaining boundary |
|---|---|---|
| §1 Curated academic supervision | Public DTOs require active scoped approval; signup/application never approves or publishes | Original 22 security cases, two API instances, complete four-role browser journey; broader subjects/home coverage require approved scope and implementation |
| §2 Stack / persistence | Go chi REST + official MongoDB Go v2, React/TypeScript/Vite, transactional actual records | Original Atlas verification; latest complete Go race run used actual local WiredTiger replica set because Atlas has 500 collections. No application fallback or Node API |
| §2 Deployment | GitHub CI, Nginx/systemd, release build; CLI development `.env` loader | Parser/precedence/production exclusion unit tests pass; actual GitHub CI/droplet/domain/TLS/restore unverified |
| §3–4 Design / bilingual responsive experience | Reviewed public/account design; distinct private sidebar/drawer, tokens, shared fields/status/errors; new tuition/account/service screens | Full 39/39 Chromium regression passed, zero flaky/skipped. Existing login/public pixel baselines preserved. Latest admin refinement: affected 2/2 browser tests plus final EN/HI desktop/mobile recapture passed |
| §3–4 Browser quality | English/Hindi, keyboard/drawer, CSS 200% reflow, loading/error/empty/retry, axe | 23 new module screenshots individually opened; latest key and four-role recaptures reinspected. Both workspace candidates reviewed; only changed English desktop promoted. All 12 pixel checks passed in the full regression. Native zoom, manual screen readers, other browsers/hardware remain unverified |
| §4 Forms / persistence | Saved requirement drafts, bounded validation, stale versions, explicit errors and loading | Original persistence/E2E plus new multi-step agreement, plan/handover, support, account, file flows; not every edit auto-saves |
| §5 Discovery | Real approved tutor search/profile, language/subject, paused availability exclusion, actual no-match requirement submission | Original API/public browser tests; class/board/locality/time/budget search, comparison/shortlist and operator matching remain pending |
| §5 Locality | Free landmark/locality input; no false GPS/distance/coverage | Purnea/Purnia accepted; structured city-scoped alias configuration and valid geospatial matching pending |
| §6 Family accounts | Adult self and guardian-controlled multiple learners, requirements, owned trial booking | Original guardian/household/persistence tests; declaration is development-only, live guardian verification blocked |
| §6–7 Tuition agreement / calendar | Immutable fee/terms snapshots, family proposal/tutor acceptance, recurring classes, reschedule/makeup, pause/cancel, balance | 9 tuition integration cases with race checks and complete four-role browser journey passed. Automated renewal, home-address workflow and approved live policies remain open |
| §7 Availability / scheduling | IANA zones, weekly windows, leave, paused offerings, buffer/capacity; shared authoritative tutor/learner guards for trials/classes | Recurrence/DST/day-boundary, spring/fall local windows, half-hour clock folds, cancellation/rebook and simultaneous two-instance overlap tests pass. Home travel rules require approved operations |
| §6–9 Plans and continuity | Assigned mentor plan versions, topic evidence/review dates, family consent, replacement acceptance, immutable handover/agreement history | Browser journey retains family plan and gets HTTP 404 for old tutor. Specialist conflicts, overdue-review operations and broader reassignment scope pending |
| §6–9 Conversations / notifications | Shared family/current tutor/mentor conversation; owner-only in-app updates, cursor pages | Idempotency/ownership/revocation integration and browser reply/reload/notification-read flows passed; external delivery not configured |
| §6–7 Private files | Application and tuition uploads, private disk/S3 documents, authenticated Cloudinary videos, signatures/size/quota/receipt, quarantine, ClamAV worker, guarded downloads/range playback | Real-Mongo ownership/quarantine/range tests and provider wire tests pass with race checks; recorded-video browser persistence passes. Live upload/scan, S3, retention/deletion/restore remain unverified; main scanner is absent |
| §7 Assessment pipeline | Staff queues/detail; assigned review and conflict declaration; Zoom API creation/rescheduling/cancellation with durable recovery and host guards; no-show; six-score evidence; scoped approval; changes/decline; suspend/reinstate/terminate; retained audit | Real-Mongo concurrency/lifecycle/provider recovery and selected full staff browser flow pass. Zoom OAuth, all five scopes and the configured host's meeting-list access are verified. Live meeting creation, email, specialist/multi-scope applications and reviewed profile changes remain pending |
| §8 Least privilege | Parent ownership; tutor assignment expiry; mentor task scope; separate support/finance/admin permissions | API negative-role tests; finance cannot browse academics, support cannot browse another person's safeguarding evidence. Staff provisioning database test passed with race checks |
| §8 Support / privacy / safeguarding | Case creation → eligible claim → response/wait/resolve → requester reopen; redacted unassigned details | 4 real-MongoDB cases and complete browser requester/operator/reopen flow passed. Priority/escalation/protective action workflow and approved staffing/legal response remain open; no automatic deletion |
| §8 Staff / audit / reports / CMS | Actual network status aggregates; paginated queues, decision history and staff lists; reviewer assignment; durable existing-booking follow-ups with manual resolution; provision-only staff CLI | Staff stale-write/role/pagination/concurrency checks pass. Permission-change UI, MFA, CMS publication, broader reports and remaining legacy cursor migration remain pending |
| §9 Genuine reviews | No invented testimonials, stars or count | Relationship-backed review/moderation/report/response workflow remains pending |
| §10–12 State transitions / concurrency | Documented application/trial/tuition/hold/payment/refund/plan/handover/case/file transitions; idempotent receipts; version guards | 51 database/security subcases passed in the latest complete Go race run, including recurring rollback, expired holds, capacity-changed capture and failed refund balance |
| §11 Schema / migration | Required-field MongoDB validators, unique/indexed receipts/history, tuition migration ledger | Local migration exercised across all test databases; new Atlas migration blocked by collection capacity. Richer schemas and fuller migration history remain open |
| §12 Outbox | Durable leases, expiry, bounded backoff/attempts, provider/scan work outside transactions, retry controls | Payment/scan integration tests; actual notification-delivery adapters and operations escalation remain open |
| §13 Email/password / account security | Argon2id, generic login failure, exact Origin, CSRF, opaque hashed sessions, signup transaction, preferences/password change/session revocation | Original authentication security cases + 3 new account cases + browser password/session rotation and new-device Hindi preference passed. Initial signup setup timeout under concurrent build/race workload did not recur when run separately |
| §13 Staff bootstrap / production gates | CLI hidden terminal password or explicit secret-manager pipe, duplicate-account protection and audit; production auth remains closed | Real-Mongo provisioning/hash/audit/duplicate protection test passed with race checks. Staff MFA, recovery, email ownership/breached-password policy and release security review remain required |
| §14 API contract | Generated OpenAPI and strict TypeScript for exposed tuition/billing/case/account/media APIs, raw webhook/download contracts | Generation/typecheck/lint/build pass; no contract claim for unimplemented domains |
| §15 Payment collection / refunds | Real Razorpay HTTPS adapter, integer server INR, checkout verification, raw-body HMAC, event dedup, late capture/refund review, separate approver, idempotent refund worker | 8 billing DB cases and 2 local wire/signature tests pass. Test-only provider seam; actual sandbox unexecuted. Approved account reported, test key/secret/webhook absent in private `.env` |
| §15 Finance records / payouts | Owner-scoped payment/refund history, balanced immutable journal, finance retry/reconciliation UI | Browser disabled-checkout and finance empty-state pass. No fake payment or settlement; commission, tax invoice, tutor earnings, disputes, Route/payout approval and actual reconciliation remain open |
| §16 Performance / SEO | Lazy private routes, self-hosted fonts, responsive WebP hero, bounded new lists | Production build and 3 component tests pass. Public pre-rendering, Lighthouse/field metrics and performance budgets remain pending |
| §17–18 Test/fixture/release checks | Actual Go/Mongo/browser tests; fictional fixture identities, test-only gateway/scanner seams | Full aggregate Chromium regression 39/39 passed; exact latest runs are recorded in progress.md. Cross-browser, full release/operator walkthrough, backup restore and live-provider verification pending |
| §19 Persistent handoff | AGENTS, full spec, progress, matrix, state machines, payment/files/staff setup and launch checklist | Maintained; unresolved items remain explicit rather than replaced with success toasts or static application data |

Atlas cleanup remains pending explicit approval: `.local/atlas-test-databases.json` proposes only 27 named automated-test databases and excludes the configured app database. No deletion was performed. The additive staff/application updates are installed on existing Atlas collections; the wider tuition/billing/file extension migrations remain blocked by capacity.

## 2026-09-25: operator-approved Atlas test database cleanup

After the droplet migration failed with AtlasError code 8000, a read-only inventory confirmed 500 collections. The user explicitly approved deletion of the 27 reviewed automated-test databases listed in `.local/atlas-test-databases.json`. An exact-name allowlist and pre-deletion count checks constrained the operation; the configured application database was excluded. All 27 databases (398 collections) were deleted. Post-deletion inventory confirmed 102 remaining collections and unchanged collection names/counts in all eight preserved application databases, including `tutor_dev`. The local audit is `.local/atlas-approved-cleanup-20260925.json`. Earlier notes describing this specific cleanup as pending approval are superseded. The droplet migration must still be rerun; this cleanup does not establish deployment or production readiness.
