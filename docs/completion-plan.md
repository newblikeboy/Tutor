# Remaining project implementation

2026-09-23 staff extension: application detail/queues, reviewer assignment/conflict declaration, persisted Zoom interview appointments, scorecards/scoped approval, reassessment, suspension/reinstatement/termination, durable follow-up outcomes and paginated audit history are implemented. Zoom host creation and external invitation delivery remain unconnected. Specialist/multi-scope review, permission administration/MFA, CMS and broader reporting remain separate pending work. See `staff-operations.md` and the latest executed checkpoint in `progress.md`.

User request: complete the pending project, preserving the approved public/account/private-workspace designs, Go/React/Atlas stack, email/password authentication and DigitalOcean deployment target. The master prompt and its identical specification copy were read in full again before extending scope.

Work is implemented as connected milestones rather than empty routes:

1. Ongoing tuition: tutor availability and exceptions, immutable agreement versions, family/tutor acceptance, recurring class reservations, cancellation/rescheduling/makeup, attendance and reviewed lesson evidence, package balance, versioned learning plans and family-authorised handover.
2. Service operations: parent-visible conversations, authorised private files, support/privacy and separately restricted safeguarding cases, academic matching/shortlists, account preferences, genuine relationship reviews, staff and content operations, bounded pagination and outbox delivery.
3. Billing: server-calculated INR orders, Razorpay collection/refund adapter, verified/deduplicated webhooks, hold expiry/late-payment handling, invoices, earnings entitlement and reconciliation. No live charge or bank settlement is implied by an internal record. User reports an approved Razorpay account; test credentials and Route/payout approval remain separate configuration gates.
4. Release checks: password recovery/change and staff MFA/provisioning, ownership and concurrency regressions, contract/migrations, English/Hindi browser review, public pre-rendering and measured performance, deployment/restore tooling and operator review.

Noncritical assumptions: existing online Mathematics scope remains the first exercised teaching offering; no coverage, fees, cancellation rules or legal text are invented as approved operating policy. New agreement rules are versioned and visibly development-only until the operator publishes reviewed terms. Existing records and secret environment values are preserved. New domains use separate collections and explicit access checks. No Docker is added.

External actions remain gated: production domain/droplet/GitHub configuration, real delivery, payment credentials, live prices/policies, guardian verification, safeguarding staffing, qualified legal review and final operator acceptance. Implementation can proceed without pretending these approvals or deliveries have happened.

Verification uses the same Go repositories and real replica-set transactions. Earlier checks used Atlas; the current extension uses an explicitly selected local WiredTiger replica set after Atlas reached its collection limit. No application fallback or completed Atlas rerun is implied. Scheduling writers contend on authoritative tutor/learner guards; external provider calls run outside retryable transactions. Reference checks: [MongoDB Go transactions](https://www.mongodb.com/docs/drivers/go/current/crud/transactions/), [OWASP authorisation](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html), [OWASP file uploads](https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html). Exact executed results belong in progress.md and the acceptance matrix.

## Current implementation boundary

Batch 1's development tuition/continuity journey is implemented and exercised. Batch 2 has conversations, in-app notifications, private files, assigned support/privacy/restricted safeguarding cases and account preferences; discovery/shortlists, matching operations, reviews, CMS and reports remain. Batch 3 has Razorpay order/verification/webhook/refund workflows, balanced capture/refund journals and finance retry/reconciliation; actual sandbox acceptance, invoice/commission/earnings/dispute/payout work remain. Batch 4 has password change/session revocation, staff provisioning and added security/browser coverage; recovery/delivery/MFA, richer schemas/history, pre-rendering/performance and the complete release review remain. None of these batches is labelled production-ready.
